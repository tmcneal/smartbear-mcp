/**
 * Generated by orval v8.4.2 🍺
 * Do not edit manually.
 * Zephyr Cloud API
 *
# Introduction

This is the **Zephyr Cloud** API. If you are looking for the Server API, please refer to the [documentation](https://support.smartbear.com/zephyr-scale-server/api-docs/v1/) for more information.

This API uses the OpenAPI specification. If you want to generate clients for different languages, please use the link above to download the API specification and use it with your preferred tools.

For feature requests or general support, please head to our [support site](https://support.smartbear.com/zephyr/message/).

# Authentication
The API authentication is based on JSON Web Token (JWT), which is a key for accessing the API. Authenticating using JWT requires the following steps:
* Generate a key
* Make authenticated requests

## Generate a Key
For accessing the API, you must generate an access key in Jira. To generate an access token, click on your profile picture at the page bottom left, and
choose the option “Zephyr API keys". For more information, please check out the [documentation](https://support.smartbear.com/zephyr/docs/en/rest-api/api-access-tokens-management.html).

## Accessing the API
The API is available at the following base URL:
```
https://api.zephyrscale.smartbear.com/v2
```
For example, the final URL for retrieving test cases would be:
```
https://api.zephyrscale.smartbear.com/v2/testcases
```
For EU region, the API can be accessed at the following base URL:
```
https://eu.api.zephyrscale.smartbear.com/v2
```

## Making Authenticated Requests
To authenticate subsequent API requests, you must provide a valid token in an HTTP header, which is the key generated on the previous step:
```
Authorization: Bearer {bearer_token}
```
# Representing Users

Previous API definitions used Atlassian User Keys to identify users. This parameter is deprecated because of privacy
changes. Use Atlassian Account ID instead.


Atlassian Account IDs are globally unique opaque identifiers for identifying a user. Every Atlassian Account has a
ID which is assigned when the account is created and is immutable. Account IDs contain no personally identifiable
information and are only used to retrieve user information ondemand. This API does not return any user information
other than the Account ID (with the exception of the deprecated user keys during the deprecation period).

 * OpenAPI spec version: 2
 */
import * as zod from "zod";

/**
 * Retrieves all test cases. Query parameters can be used to filter the results. If the user who is preforming the API call has access to more than 1000 projects, the `projectKey` query parameter is mandatory.

 * @summary Get test cases
 */
export const listTestCasesQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const listTestCasesQueryMaxResultsDefault = 10;

export const listTestCasesQueryStartAtDefault = 0;
export const listTestCasesQueryStartAtMin = 0;
export const listTestCasesQueryStartAtMax = 1000000;

export const ListTestCasesQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestCasesQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  folderId: zod.number().min(1).optional().describe("Folder ID filter"),
  maxResults: zod
    .number()
    .min(1)
    .default(listTestCasesQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listTestCasesQueryStartAtMin)
    .max(listTestCasesQueryStartAtMax)
    .default(listTestCasesQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listTestCases200ResponseOneStartAtMin = 0;

export const listTestCases200ResponseOneTotalMin = 0;

export const listTestCases200ResponseTwoValuesItemKeyRegExp = /.+-T[0-9]+/;
export const listTestCases200ResponseTwoValuesItemNameMax = 255;

export const listTestCases200ResponseTwoValuesItemNameRegExp = /^(?!\\s*$).+/;

export const listTestCases200ResponseTwoValuesItemEstimatedTimeMin = 0;

export const listTestCases200ResponseTwoValuesItemLabelsMax = 50;

export const listTestCases200ResponseTwoValuesItemOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const ListTestCases200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listTestCases200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listTestCases200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(listTestCases200ResponseTwoValuesItemKeyRegExp)
              .describe("The test case key"),
            name: zod
              .string()
              .min(1)
              .max(listTestCases200ResponseTwoValuesItemNameMax)
              .regex(listTestCases200ResponseTwoValuesItemNameRegExp),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            createdOn: zod
              .string()
              .datetime({})
              .optional()
              .describe(
                "Data and time test case was created. Format: yyyy-MM-dd'T'HH:mm:ss'Z'. This field is read-only, cannot be updated.\n",
              ),
            objective: zod
              .string()
              .nullish()
              .describe("A description of the objective."),
            precondition: zod
              .string()
              .nullish()
              .describe("Any conditions that need to be met."),
            estimatedTime: zod
              .number()
              .min(listTestCases200ResponseTwoValuesItemEstimatedTimeMin)
              .nullish()
              .describe("Estimated duration in milliseconds."),
            labels: zod
              .array(zod.string())
              .max(listTestCases200ResponseTwoValuesItemLabelsMax)
              .optional()
              .describe("Array of labels associated to this entity."),
            component: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            priority: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            status: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            folder: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            owner: zod
              .object({
                accountId: zod
                  .string()
                  .regex(
                    listTestCases200ResponseTwoValuesItemOwnerAccountIdRegExp,
                  )
                  .nullable()
                  .describe("Atlassian Account ID of the Jira user."),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira REST API endpoint to get the full representation of the Jira user.",
                  ),
              })
              .strict()
              .nullish(),
            testScript: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .optional(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            links: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
                webLinks: zod
                  .array(
                    zod
                      .object({
                        description: zod
                          .string()
                          .optional()
                          .describe("The web link description"),
                        url: zod.string().url().describe("The web link URL"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                          ),
                        id: zod.number().min(1).optional(),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of web links for this entity"),
              })
              .strict()
              .optional()
              .describe("This property is ignored on updates."),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListTestCasesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a test case. Fields `priorityName` and `statusName` will be set to default values if not informed. Default values are usually “Normal” for `priorityName` and “Draft” for `statusName`. All required test case custom fields should be present in the request.

 * @summary Create test case
 */
export const createTestCaseBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createTestCaseBodyNameMax = 255;

export const createTestCaseBodyNameRegExp = /^(?!\\s*$).+/;
export const createTestCaseBodyEstimatedTimeMin = 0;

export const createTestCaseBodyComponentIdMin = 0;

export const createTestCaseBodyPriorityNameMax = 255;

export const createTestCaseBodyStatusNameMax = 255;

export const createTestCaseBodyOwnerIdRegExp = /^[-:a-zA-Z0-9]{1,128}$/;
export const createTestCaseBodyLabelsMax = 50;

export const CreateTestCaseBody = zod.object({
  projectKey: zod
    .string()
    .regex(createTestCaseBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createTestCaseBodyNameMax)
    .regex(createTestCaseBodyNameRegExp),
  objective: zod.string().nullish().describe("A description of the objective."),
  precondition: zod
    .string()
    .nullish()
    .describe("Any conditions that need to be met."),
  estimatedTime: zod
    .number()
    .min(createTestCaseBodyEstimatedTimeMin)
    .optional()
    .describe("Estimated duration in milliseconds."),
  componentId: zod
    .number()
    .min(createTestCaseBodyComponentIdMin)
    .optional()
    .describe("ID of a component from Jira."),
  priorityName: zod
    .string()
    .min(1)
    .max(createTestCaseBodyPriorityNameMax)
    .optional()
    .describe("The priority name."),
  statusName: zod
    .string()
    .min(1)
    .max(createTestCaseBodyStatusNameMax)
    .optional()
    .describe("The status name."),
  folderId: zod
    .number()
    .min(1)
    .optional()
    .describe("ID of a folder to place the entity within."),
  ownerId: zod
    .string()
    .regex(createTestCaseBodyOwnerIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  labels: zod
    .array(zod.string())
    .max(createTestCaseBodyLabelsMax)
    .optional()
    .describe("Array of labels associated to this entity."),
  customFields: zod
    .record(zod.string(), zod.unknown())
    .optional()
    .describe(
      "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
    ),
});

export const CreateTestCase201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
    key: zod.string().optional(),
  })
  .strict();

export const CreateTestCaseDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Retrieves all test cases. Query parameters can be used to filter the results.

Use this endpoint for retrieving large volumes of test cases.

Results are split in different pages, of size equal to the `limit` query parameter.
To fetch the next page, use the `next` URL returned in the response.
It should have the same parameters as the original request, with the updated value for `startAtId` parameter.
Alternatively, the same request can be made with the `startAtId` parameter set to the `nextStartId` value
returned in the response.

 * @summary Get test cases (NextGen)
 */
export const listTestCasesCursorPaginatedQueryProjectKeyRegExp =
  /([A-Z][A-Z_0-9]+)/;

export const listTestCasesCursorPaginatedQueryLimitDefault = 10;
export const listTestCasesCursorPaginatedQueryLimitMax = 1000;

export const listTestCasesCursorPaginatedQueryStartAtIdDefault = 0;
export const listTestCasesCursorPaginatedQueryStartAtIdMin = 0;

export const ListTestCasesCursorPaginatedQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestCasesCursorPaginatedQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  folderId: zod.number().min(1).optional().describe("Folder ID filter"),
  limit: zod
    .number()
    .min(1)
    .max(listTestCasesCursorPaginatedQueryLimitMax)
    .default(listTestCasesCursorPaginatedQueryLimitDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the limit value in the response to confirm how many results were actually returned.\n",
    ),
  startAtId: zod
    .number()
    .min(listTestCasesCursorPaginatedQueryStartAtIdMin)
    .default(listTestCasesCursorPaginatedQueryStartAtIdDefault)
    .describe("Zero-indexed starting position for ID-based pagination."),
});

export const listTestCasesCursorPaginated200ResponseOneNextStartAtIdMin = 0;

export const listTestCasesCursorPaginated200ResponseOneLimitMin = 0;

export const listTestCasesCursorPaginated200ResponseTwoValuesItemKeyRegExp =
  /.+-T[0-9]+/;
export const listTestCasesCursorPaginated200ResponseTwoValuesItemNameMax = 255;

export const listTestCasesCursorPaginated200ResponseTwoValuesItemNameRegExp =
  /^(?!\\s*$).+/;

export const listTestCasesCursorPaginated200ResponseTwoValuesItemEstimatedTimeMin = 0;

export const listTestCasesCursorPaginated200ResponseTwoValuesItemLabelsMax = 50;

export const listTestCasesCursorPaginated200ResponseTwoValuesItemOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const ListTestCasesCursorPaginated200Response = zod
  .object({
    next: zod.string().url().nullish(),
    nextStartAtId: zod
      .number()
      .min(listTestCasesCursorPaginated200ResponseOneNextStartAtIdMin)
      .nullable(),
    limit: zod.number().min(listTestCasesCursorPaginated200ResponseOneLimitMin),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(
                listTestCasesCursorPaginated200ResponseTwoValuesItemKeyRegExp,
              )
              .describe("The test case key"),
            name: zod
              .string()
              .min(1)
              .max(listTestCasesCursorPaginated200ResponseTwoValuesItemNameMax)
              .regex(
                listTestCasesCursorPaginated200ResponseTwoValuesItemNameRegExp,
              ),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            createdOn: zod
              .string()
              .datetime({})
              .optional()
              .describe(
                "Data and time test case was created. Format: yyyy-MM-dd'T'HH:mm:ss'Z'. This field is read-only, cannot be updated.\n",
              ),
            objective: zod
              .string()
              .nullish()
              .describe("A description of the objective."),
            precondition: zod
              .string()
              .nullish()
              .describe("Any conditions that need to be met."),
            estimatedTime: zod
              .number()
              .min(
                listTestCasesCursorPaginated200ResponseTwoValuesItemEstimatedTimeMin,
              )
              .nullish()
              .describe("Estimated duration in milliseconds."),
            labels: zod
              .array(zod.string())
              .max(
                listTestCasesCursorPaginated200ResponseTwoValuesItemLabelsMax,
              )
              .optional()
              .describe("Array of labels associated to this entity."),
            component: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            priority: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            status: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            folder: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            owner: zod
              .object({
                accountId: zod
                  .string()
                  .regex(
                    listTestCasesCursorPaginated200ResponseTwoValuesItemOwnerAccountIdRegExp,
                  )
                  .nullable()
                  .describe("Atlassian Account ID of the Jira user."),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira REST API endpoint to get the full representation of the Jira user.",
                  ),
              })
              .strict()
              .nullish(),
            testScript: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .optional(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            links: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
                webLinks: zod
                  .array(
                    zod
                      .object({
                        description: zod
                          .string()
                          .optional()
                          .describe("The web link description"),
                        url: zod.string().url().describe("The web link URL"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                          ),
                        id: zod.number().min(1).optional(),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of web links for this entity"),
              })
              .strict()
              .optional()
              .describe("This property is ignored on updates."),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListTestCasesCursorPaginatedDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a test case for the given key.
 * @summary Get test case
 */
export const getTestCasePathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const GetTestCaseParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(getTestCasePathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const getTestCase200ResponseKeyRegExp = /.+-T[0-9]+/;
export const getTestCase200ResponseNameMax = 255;

export const getTestCase200ResponseNameRegExp = /^(?!\\s*$).+/;

export const getTestCase200ResponseEstimatedTimeMin = 0;

export const getTestCase200ResponseLabelsMax = 50;

export const getTestCase200ResponseOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const GetTestCase200Response = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(getTestCase200ResponseKeyRegExp)
      .describe("The test case key"),
    name: zod
      .string()
      .min(1)
      .max(getTestCase200ResponseNameMax)
      .regex(getTestCase200ResponseNameRegExp),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    createdOn: zod
      .string()
      .datetime({})
      .optional()
      .describe(
        "Data and time test case was created. Format: yyyy-MM-dd'T'HH:mm:ss'Z'. This field is read-only, cannot be updated.\n",
      ),
    objective: zod
      .string()
      .nullish()
      .describe("A description of the objective."),
    precondition: zod
      .string()
      .nullish()
      .describe("Any conditions that need to be met."),
    estimatedTime: zod
      .number()
      .min(getTestCase200ResponseEstimatedTimeMin)
      .nullish()
      .describe("Estimated duration in milliseconds."),
    labels: zod
      .array(zod.string())
      .max(getTestCase200ResponseLabelsMax)
      .optional()
      .describe("Array of labels associated to this entity."),
    component: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    priority: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    status: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    folder: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    owner: zod
      .object({
        accountId: zod
          .string()
          .regex(getTestCase200ResponseOwnerAccountIdRegExp)
          .nullable()
          .describe("Atlassian Account ID of the Jira user."),
        self: zod
          .string()
          .url()
          .optional()
          .describe(
            "The Jira REST API endpoint to get the full representation of the Jira user.",
          ),
      })
      .strict()
      .nullish(),
    testScript: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .optional(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    links: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the entity and the Jira issue.",
                  ),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira Cloud REST API endpoint to get the full representation of the issue",
                  ),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
        webLinks: zod
          .array(
            zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The web link description"),
                url: zod.string().url().describe("The web link URL"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                  ),
                id: zod.number().min(1).optional(),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of web links for this entity"),
      })
      .strict()
      .optional()
      .describe("This property is ignored on updates."),
  })
  .strict();

export const GetTestCaseDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Updates an existing test case.  Please take into account that for each non-specified field the value will be cleared. If the project has test case custom fields, all custom fields should be present in the request. To leave any of them blank, please set them null if they are not required custom fields.

 * @summary Update test case
 */
export const updateTestCasePathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const UpdateTestCaseParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(updateTestCasePathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const updateTestCaseBodyKeyRegExp = /.+-T[0-9]+/;
export const updateTestCaseBodyNameMax = 255;

export const updateTestCaseBodyNameRegExp = /^(?!\\s*$).+/;

export const updateTestCaseBodyEstimatedTimeMin = 0;

export const updateTestCaseBodyLabelsMax = 50;

export const updateTestCaseBodyOwnerAccountIdRegExp = /^[-:a-zA-Z0-9]{1,128}$/;

export const UpdateTestCaseBody = zod.object({
  id: zod.number().min(1),
  key: zod
    .string()
    .regex(updateTestCaseBodyKeyRegExp)
    .describe("The test case key"),
  name: zod
    .string()
    .min(1)
    .max(updateTestCaseBodyNameMax)
    .regex(updateTestCaseBodyNameRegExp),
  project: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .describe("ID and link relative to Zephyr project."),
  createdOn: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Data and time test case was created. Format: yyyy-MM-dd'T'HH:mm:ss'Z'. This field is read-only, cannot be updated.\n",
    ),
  objective: zod.string().nullish().describe("A description of the objective."),
  precondition: zod
    .string()
    .nullish()
    .describe("Any conditions that need to be met."),
  estimatedTime: zod
    .number()
    .min(updateTestCaseBodyEstimatedTimeMin)
    .nullish()
    .describe("Estimated duration in milliseconds."),
  labels: zod
    .array(zod.string())
    .max(updateTestCaseBodyLabelsMax)
    .optional()
    .describe("Array of labels associated to this entity."),
  component: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .nullish(),
  priority: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    ),
  status: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    ),
  folder: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .nullish(),
  owner: zod
    .object({
      accountId: zod
        .string()
        .regex(updateTestCaseBodyOwnerAccountIdRegExp)
        .nullable()
        .describe("Atlassian Account ID of the Jira user."),
      self: zod
        .string()
        .url()
        .optional()
        .describe(
          "The Jira REST API endpoint to get the full representation of the Jira user.",
        ),
    })
    .nullish(),
  testScript: zod
    .object({
      self: zod
        .string()
        .url()
        .optional()
        .describe("The REST API endpoint to get more resource details."),
    })
    .optional(),
  customFields: zod
    .record(zod.string(), zod.unknown())
    .optional()
    .describe(
      "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
    ),
  links: zod
    .object({
      self: zod
        .string()
        .url()
        .optional()
        .describe("The REST API endpoint to get more resource details."),
    })
    .and(
      zod.object({
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
              })
              .and(
                zod.object({
                  self: zod
                    .string()
                    .url()
                    .optional()
                    .describe(
                      "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                    ),
                  id: zod
                    .number()
                    .min(1)
                    .optional()
                    .describe(
                      "The ID that represents the link between the entity and the Jira issue.",
                    ),
                  target: zod
                    .string()
                    .url()
                    .optional()
                    .describe(
                      "The Jira Cloud REST API endpoint to get the full representation of the issue",
                    ),
                  type: zod
                    .enum(["COVERAGE", "BLOCKS", "RELATED"])
                    .optional()
                    .describe("The link type"),
                }),
              ),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
        webLinks: zod
          .array(
            zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The web link description"),
                url: zod.string().url().describe("The web link URL"),
              })
              .and(
                zod.object({
                  self: zod
                    .string()
                    .url()
                    .optional()
                    .describe(
                      "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                    ),
                  id: zod.number().min(1).optional(),
                  type: zod
                    .enum(["COVERAGE", "BLOCKS", "RELATED"])
                    .optional()
                    .describe("The link type"),
                }),
              ),
          )
          .optional()
          .describe("A list of web links for this entity"),
      }),
    )
    .optional()
    .describe("This property is ignored on updates."),
});

export const UpdateTestCaseDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns links for a test case with specified key.
 * @summary Get links
 */
export const getTestCaseLinksPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const GetTestCaseLinksParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(getTestCaseLinksPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const GetTestCaseLinks200Response = zod
  .object({
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
    issues: zod
      .array(
        zod
          .object({
            issueId: zod.number().min(1).describe("The Jira issue ID"),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
              ),
            id: zod
              .number()
              .min(1)
              .optional()
              .describe(
                "The ID that represents the link between the entity and the Jira issue.",
              ),
            target: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Jira Cloud REST API endpoint to get the full representation of the issue",
              ),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
          })
          .strict(),
      )
      .optional()
      .describe("A list of Jira issues linked to this entity"),
    webLinks: zod
      .array(
        zod
          .object({
            description: zod
              .string()
              .optional()
              .describe("The web link description"),
            url: zod.string().url().describe("The web link URL"),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
              ),
            id: zod.number().min(1).optional(),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
          })
          .strict(),
      )
      .optional()
      .describe("A list of web links for this entity"),
  })
  .strict()
  .describe("This property is ignored on updates.");

export const GetTestCaseLinksDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test case and a Jira issue.
 * @summary Create issue link
 */
export const createTestCaseIssueLinkPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const CreateTestCaseIssueLinkParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(createTestCaseIssueLinkPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const CreateTestCaseIssueLinkBody = zod.object({
  issueId: zod.number().min(1).describe("The Jira issue ID"),
});

export const CreateTestCaseIssueLink201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestCaseIssueLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test case and a generic URL.
 * @summary Create web link
 */
export const createTestCaseWebLinkPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const CreateTestCaseWebLinkParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(createTestCaseWebLinkPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const CreateTestCaseWebLinkBody = zod.object({
  description: zod.string().optional().describe("The web link description"),
  url: zod.string().url().describe("The web link URL"),
});

export const CreateTestCaseWebLink201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestCaseWebLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all test case versions for a test case with specified key. Response is ordered by most recent first.
 * @summary Get versions
 */
export const listTestCaseVersionsPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const ListTestCaseVersionsParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(listTestCaseVersionsPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const listTestCaseVersionsQueryMaxResultsDefault = 10;

export const listTestCaseVersionsQueryStartAtDefault = 0;
export const listTestCaseVersionsQueryStartAtMin = 0;
export const listTestCaseVersionsQueryStartAtMax = 1000000;

export const ListTestCaseVersionsQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(listTestCaseVersionsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listTestCaseVersionsQueryStartAtMin)
    .max(listTestCaseVersionsQueryStartAtMax)
    .default(listTestCaseVersionsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listTestCaseVersions200ResponseOneStartAtMin = 0;

export const listTestCaseVersions200ResponseOneTotalMin = 0;

export const ListTestCaseVersions200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listTestCaseVersions200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listTestCaseVersions200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            self: zod
              .string()
              .url()
              .optional()
              .describe("The REST API endpoint to get more resource details."),
            id: zod.number().min(1).optional(),
          })
          .strict(),
      )
      .optional()
      .describe("A list of versions for a test case"),
  })
  .strict();

export const ListTestCaseVersionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Retrieves a specific version of a test case.
 * @summary Get version
 */
export const getTestCaseVersionPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const GetTestCaseVersionParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(getTestCaseVersionPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
  version: zod.number().describe("Version of the test case to retrieve."),
});

export const getTestCaseVersion200ResponseKeyRegExp = /.+-T[0-9]+/;
export const getTestCaseVersion200ResponseNameMax = 255;

export const getTestCaseVersion200ResponseNameRegExp = /^(?!\\s*$).+/;

export const getTestCaseVersion200ResponseEstimatedTimeMin = 0;

export const getTestCaseVersion200ResponseLabelsMax = 50;

export const getTestCaseVersion200ResponseOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const GetTestCaseVersion200Response = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(getTestCaseVersion200ResponseKeyRegExp)
      .describe("The test case key"),
    name: zod
      .string()
      .min(1)
      .max(getTestCaseVersion200ResponseNameMax)
      .regex(getTestCaseVersion200ResponseNameRegExp),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    createdOn: zod
      .string()
      .datetime({})
      .optional()
      .describe(
        "Data and time test case was created. Format: yyyy-MM-dd'T'HH:mm:ss'Z'. This field is read-only, cannot be updated.\n",
      ),
    objective: zod
      .string()
      .nullish()
      .describe("A description of the objective."),
    precondition: zod
      .string()
      .nullish()
      .describe("Any conditions that need to be met."),
    estimatedTime: zod
      .number()
      .min(getTestCaseVersion200ResponseEstimatedTimeMin)
      .nullish()
      .describe("Estimated duration in milliseconds."),
    labels: zod
      .array(zod.string())
      .max(getTestCaseVersion200ResponseLabelsMax)
      .optional()
      .describe("Array of labels associated to this entity."),
    component: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    priority: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    status: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    folder: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    owner: zod
      .object({
        accountId: zod
          .string()
          .regex(getTestCaseVersion200ResponseOwnerAccountIdRegExp)
          .nullable()
          .describe("Atlassian Account ID of the Jira user."),
        self: zod
          .string()
          .url()
          .optional()
          .describe(
            "The Jira REST API endpoint to get the full representation of the Jira user.",
          ),
      })
      .strict()
      .nullish(),
    testScript: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .optional(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    links: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the entity and the Jira issue.",
                  ),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira Cloud REST API endpoint to get the full representation of the issue",
                  ),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
        webLinks: zod
          .array(
            zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The web link description"),
                url: zod.string().url().describe("The web link URL"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                  ),
                id: zod.number().min(1).optional(),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of web links for this entity"),
      })
      .strict()
      .optional()
      .describe("This property is ignored on updates."),
  })
  .strict();

export const GetTestCaseVersionDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns the test script for the given test case
 * @summary Get test script
 */
export const getTestCaseTestScriptPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const GetTestCaseTestScriptParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(getTestCaseTestScriptPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const GetTestCaseTestScript200Response = zod
  .object({
    type: zod
      .enum(["plain", "bdd"])
      .describe(
        "Test scripts can be written in plain text or BDD format. The BDD type supports\nremote execution on a build system via API plugin.\n\nSupported Keywords for BDD:\nGiven, When, Then, And, But.\n\nFor more information about BDD and Gherkin syntax, see:\nhttps://support.smartbear.com/zephyr/docs/en/test-cases/gherkin-behavior-driven-development--bdd-.html\n\nFor Plain Text scripts, we support HTML fragments.\nTo create a step-by-step test script, you should use the POST /testcases/{testCaseKey}/teststeps endpoint.\n",
      ),
    text: zod.string().min(1),
    id: zod.number().min(1).optional(),
  })
  .strict()
  .describe("Response body when retrieving test scripts");

export const GetTestCaseTestScriptDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates or updates the test script for a test case. If the test case currently has a sequence of test steps assigned to it, these will be implicitly removed.
 * @summary Create test script
 */
export const createTestCaseTestScriptPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const CreateTestCaseTestScriptParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(createTestCaseTestScriptPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const CreateTestCaseTestScriptBody = zod
  .object({
    type: zod
      .enum(["plain", "bdd"])
      .describe(
        "Test scripts can be written in plain text or BDD format. The BDD type supports\nremote execution on a build system via API plugin.\n\nSupported Keywords for BDD:\nGiven, When, Then, And, But.\n\nFor more information about BDD and Gherkin syntax, see:\nhttps://support.smartbear.com/zephyr/docs/en/test-cases/gherkin-behavior-driven-development--bdd-.html\n\nFor Plain Text scripts, we support HTML fragments.\nTo create a step-by-step test script, you should use the POST /testcases/{testCaseKey}/teststeps endpoint.\n",
      ),
    text: zod.string().min(1),
  })
  .describe("Request body for creating test scripts");

export const CreateTestCaseTestScript201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestCaseTestScriptDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns the test steps for the given test case. Provides a paged response, with 100 items per page.
 * @summary Get test steps
 */
export const getTestCaseTestStepsPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const GetTestCaseTestStepsParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(getTestCaseTestStepsPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const getTestCaseTestStepsQueryMaxResultsDefault = 10;

export const getTestCaseTestStepsQueryStartAtDefault = 0;
export const getTestCaseTestStepsQueryStartAtMin = 0;
export const getTestCaseTestStepsQueryStartAtMax = 1000000;

export const GetTestCaseTestStepsQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(getTestCaseTestStepsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(getTestCaseTestStepsQueryStartAtMin)
    .max(getTestCaseTestStepsQueryStartAtMax)
    .default(getTestCaseTestStepsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const getTestCaseTestSteps200ResponseOneStartAtMin = 0;

export const getTestCaseTestSteps200ResponseOneTotalMin = 0;

export const getTestCaseTestSteps200ResponseTwoValuesItemTestCaseTwoTestCaseKeyRegExp =
  /(.+-T[0-9]+)/;

export const GetTestCaseTestSteps200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(getTestCaseTestSteps200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(getTestCaseTestSteps200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            inline: zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The instruction to be followed"),
                testData: zod
                  .string()
                  .optional()
                  .describe(
                    "Any test data required to perform the instruction (optional). The fields values provided can be interpolated into the description.",
                  ),
                expectedResult: zod
                  .string()
                  .optional()
                  .describe(
                    "The expected outcome of executing the instruction",
                  ),
                customFields: zod
                  .record(zod.string(), zod.unknown())
                  .optional()
                  .describe(
                    "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
                  ),
                reflectRef: zod
                  .string()
                  .optional()
                  .describe("The AI reference. Zephyr only feature"),
              })
              .strict()
              .optional(),
            testCase: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                testCaseKey: zod
                  .string()
                  .regex(
                    getTestCaseTestSteps200ResponseTwoValuesItemTestCaseTwoTestCaseKeyRegExp,
                  )
                  .optional()
                  .describe(
                    "The key of the other test case that the test step should delegate execution to. This cannot be the parent test case.",
                  ),
                parameters: zod
                  .array(
                    zod
                      .object({
                        name: zod
                          .string()
                          .optional()
                          .describe("Name of the parameter"),
                        type: zod
                          .enum(["MANUAL_INPUT", "DEFAULT_VALUE"])
                          .optional()
                          .describe(
                            "Type of the parameter. It is manual inputs for parameters or the default values.",
                          ),
                        value: zod
                          .string()
                          .optional()
                          .describe("Value of the parameter"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("The list of parameters of the call to test step"),
              })
              .strict()
              .optional(),
          })
          .strict()
          .describe(
            "An instruction to be followed as part of a step-by-step test script. The test step can have either an inline definition, or delegate execution to another test case. One of these options must be specified.",
          ),
      )
      .optional()
      .describe("The list of test steps"),
  })
  .strict()
  .describe("Response body when retrieving test steps");

export const GetTestCaseTestStepsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Assigns a series of test steps to a test case, appending them to any existing sequence of test steps. A maximum of 100 steps can be posted per request. Consumers should not attempt to parallelize this operation, as the order of the steps is defined by the input order. If this endpoint is called on a test case that already has a plain text or BDD test script, that test script will implicitly be removed. All required step custom fields should be present in the request.

 * @summary Post test steps
 */
export const createTestCaseTestStepsPathTestCaseKeyRegExp = /(.+-T[0-9]+)/;

export const CreateTestCaseTestStepsParams = zod.object({
  testCaseKey: zod
    .string()
    .regex(createTestCaseTestStepsPathTestCaseKeyRegExp)
    .describe(
      "The key of the test case. Test case keys are of the format [A-Z]+-T[0-9]+",
    ),
});

export const createTestCaseTestStepsBodyItemsItemTestCaseTwoTestCaseKeyRegExp =
  /(.+-T[0-9]+)/;

export const CreateTestCaseTestStepsBody = zod
  .object({
    mode: zod
      .string()
      .describe(
        'Valid values: `"APPEND"`, `"OVERWRITE"`. <br> `OVERWRITE` deletes and recreates the test steps and associated custom field values using the provided input. Attachments for existing steps are kept, but those for missing steps are deleted permanently <br> `APPEND` only adds extra steps to your test steps.\n',
      ),
    items: zod
      .array(
        zod
          .object({
            inline: zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The instruction to be followed"),
                testData: zod
                  .string()
                  .optional()
                  .describe(
                    "Any test data required to perform the instruction (optional). The fields values provided can be interpolated into the description.",
                  ),
                expectedResult: zod
                  .string()
                  .optional()
                  .describe(
                    "The expected outcome of executing the instruction",
                  ),
                customFields: zod
                  .record(zod.string(), zod.unknown())
                  .optional()
                  .describe(
                    "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
                  ),
                reflectRef: zod
                  .string()
                  .optional()
                  .describe("The AI reference. Zephyr only feature"),
              })
              .optional(),
            testCase: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .and(
                zod.object({
                  testCaseKey: zod
                    .string()
                    .regex(
                      createTestCaseTestStepsBodyItemsItemTestCaseTwoTestCaseKeyRegExp,
                    )
                    .optional()
                    .describe(
                      "The key of the other test case that the test step should delegate execution to. This cannot be the parent test case.",
                    ),
                  parameters: zod
                    .array(
                      zod.object({
                        name: zod
                          .string()
                          .optional()
                          .describe("Name of the parameter"),
                        type: zod
                          .enum(["MANUAL_INPUT", "DEFAULT_VALUE"])
                          .optional()
                          .describe(
                            "Type of the parameter. It is manual inputs for parameters or the default values.",
                          ),
                        value: zod
                          .string()
                          .optional()
                          .describe("Value of the parameter"),
                      }),
                    )
                    .optional()
                    .describe(
                      "The list of parameters of the call to test step",
                    ),
                }),
              )
              .optional(),
          })
          .describe(
            "An instruction to be followed as part of a step-by-step test script. The test step can have either an inline definition, or delegate execution to another test case. One of these options must be specified.",
          ),
      )
      .describe(
        "The list of test steps. Each step should be an object containing `inline` or `testCase`. **They should only include one of these fields at a time**.",
      ),
  })
  .describe(
    "Request body for creating test steps. A maximum of 100 test steps can be added in one request. To add more than 100 steps, you can submit multiple requests. The mode property allows you to specify whether you want to delete the current test steps and create a new list, or append more items to the end of the current list.",
  );

export const CreateTestCaseTestSteps201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestCaseTestStepsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all test cycles. Query parameters can be used to filter by project and folder.
 * @summary Get test cycles
 */
export const listTestCyclesQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const listTestCyclesQueryMaxResultsDefault = 10;

export const listTestCyclesQueryStartAtDefault = 0;
export const listTestCyclesQueryStartAtMin = 0;
export const listTestCyclesQueryStartAtMax = 1000000;

export const ListTestCyclesQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestCyclesQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  folderId: zod.number().min(1).optional().describe("Folder ID filter"),
  jiraProjectVersionId: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
    ),
  maxResults: zod
    .number()
    .min(1)
    .default(listTestCyclesQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listTestCyclesQueryStartAtMin)
    .max(listTestCyclesQueryStartAtMax)
    .default(listTestCyclesQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listTestCycles200ResponseOneStartAtMin = 0;

export const listTestCycles200ResponseOneTotalMin = 0;

export const listTestCycles200ResponseTwoValuesItemKeyRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const listTestCycles200ResponseTwoValuesItemNameRegExp = /^(?!\s*$).+/;

export const listTestCycles200ResponseTwoValuesItemOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const ListTestCycles200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listTestCycles200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listTestCycles200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(listTestCycles200ResponseTwoValuesItemKeyRegExp)
              .describe("Unique key of the test cycle"),
            name: zod
              .string()
              .regex(listTestCycles200ResponseTwoValuesItemNameRegExp)
              .describe("Name of the Test Cycle"),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            jiraProjectVersion: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish()
              .describe(
                "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
              ),
            status: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            folder: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            description: zod
              .string()
              .nullish()
              .describe("Description outlining the scope."),
            plannedStartDate: zod
              .string()
              .datetime({})
              .nullish()
              .describe(
                "Planned start date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
              ),
            plannedEndDate: zod
              .string()
              .datetime({})
              .nullish()
              .describe(
                "The planned end date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
              ),
            owner: zod
              .object({
                accountId: zod
                  .string()
                  .regex(
                    listTestCycles200ResponseTwoValuesItemOwnerAccountIdRegExp,
                  )
                  .nullable()
                  .describe("Atlassian Account ID of the Jira user."),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira REST API endpoint to get the full representation of the Jira user.",
                  ),
              })
              .strict()
              .nullish(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            links: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
                webLinks: zod
                  .array(
                    zod
                      .object({
                        description: zod
                          .string()
                          .optional()
                          .describe("The web link description"),
                        url: zod.string().url().describe("The web link URL"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                          ),
                        id: zod.number().min(1).optional(),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of web links for this entity"),
                testPlans: zod
                  .array(
                    zod
                      .object({
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the Test Cycle and the Test Plan.",
                          ),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        testPlanId: zod
                          .number()
                          .optional()
                          .describe("The ID of the test plan"),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint to get the full representation of the test plan",
                          ),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of test plans linked to a test cycle"),
              })
              .strict()
              .optional()
              .describe(
                "Represents all links that a Test Cycle has. This property is ignored on update operations.",
              ),
          })
          .strict()
          .describe("Details of a test cycle"),
      )
      .optional(),
  })
  .strict();

export const ListTestCyclesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a Test Cycle. All required test cycle custom fields should be present in the request.
 * @summary Create a Test Cycle
 */
export const createTestCycleBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createTestCycleBodyNameMax = 255;

export const createTestCycleBodyNameRegExp = /^(?!\\s*$).+/;

export const createTestCycleBodyStatusNameMax = 255;

export const createTestCycleBodyOwnerIdRegExp = /^[-:a-zA-Z0-9]{1,128}$/;

export const CreateTestCycleBody = zod.object({
  projectKey: zod
    .string()
    .regex(createTestCycleBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createTestCycleBodyNameMax)
    .regex(createTestCycleBodyNameRegExp),
  description: zod
    .string()
    .nullish()
    .describe("Description outlining the scope."),
  plannedStartDate: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Planned start date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  plannedEndDate: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "The planned end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  jiraProjectVersion: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
    ),
  statusName: zod
    .string()
    .min(1)
    .max(createTestCycleBodyStatusNameMax)
    .optional()
    .describe("The status name."),
  folderId: zod
    .number()
    .min(1)
    .optional()
    .describe("ID of a folder to place the entity within."),
  ownerId: zod
    .string()
    .regex(createTestCycleBodyOwnerIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  customFields: zod
    .record(zod.string(), zod.unknown())
    .optional()
    .describe(
      "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
    ),
});

export const CreateTestCycle201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
    key: zod.string().optional(),
  })
  .strict();

export const CreateTestCycleDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a test cycle for the given key.
 * @summary Get test cycle
 */
export const getTestCyclePathTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const GetTestCycleParams = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(getTestCyclePathTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const getTestCycle200ResponseKeyRegExp = /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const getTestCycle200ResponseNameRegExp = /^(?!\s*$).+/;

export const getTestCycle200ResponseOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const GetTestCycle200Response = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(getTestCycle200ResponseKeyRegExp)
      .describe("Unique key of the test cycle"),
    name: zod
      .string()
      .regex(getTestCycle200ResponseNameRegExp)
      .describe("Name of the Test Cycle"),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    jiraProjectVersion: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish()
      .describe(
        "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
      ),
    status: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    folder: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    description: zod
      .string()
      .nullish()
      .describe("Description outlining the scope."),
    plannedStartDate: zod
      .string()
      .datetime({})
      .nullish()
      .describe(
        "Planned start date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
      ),
    plannedEndDate: zod
      .string()
      .datetime({})
      .nullish()
      .describe(
        "The planned end date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
      ),
    owner: zod
      .object({
        accountId: zod
          .string()
          .regex(getTestCycle200ResponseOwnerAccountIdRegExp)
          .nullable()
          .describe("Atlassian Account ID of the Jira user."),
        self: zod
          .string()
          .url()
          .optional()
          .describe(
            "The Jira REST API endpoint to get the full representation of the Jira user.",
          ),
      })
      .strict()
      .nullish(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    links: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the entity and the Jira issue.",
                  ),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira Cloud REST API endpoint to get the full representation of the issue",
                  ),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
        webLinks: zod
          .array(
            zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The web link description"),
                url: zod.string().url().describe("The web link URL"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                  ),
                id: zod.number().min(1).optional(),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of web links for this entity"),
        testPlans: zod
          .array(
            zod
              .object({
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the Test Cycle and the Test Plan.",
                  ),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                testPlanId: zod
                  .number()
                  .optional()
                  .describe("The ID of the test plan"),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint to get the full representation of the test plan",
                  ),
              })
              .strict(),
          )
          .optional()
          .describe("A list of test plans linked to a test cycle"),
      })
      .strict()
      .optional()
      .describe(
        "Represents all links that a Test Cycle has. This property is ignored on update operations.",
      ),
  })
  .strict()
  .describe("Details of a test cycle");

export const GetTestCycleDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Updates an existing test cycle.  Please take into account that for each non-specified field the value will be cleared. If the project has test cycle custom fields, all custom fields should be present in the request. To leave any of them blank, please set them null if they are not required custom fields.

 * @summary Update test cycle
 */
export const updateTestCyclePathTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const UpdateTestCycleParams = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(updateTestCyclePathTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const updateTestCycleBodyKeyRegExp = /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const updateTestCycleBodyNameRegExp = /^(?!\s*$).+/;

export const updateTestCycleBodyOwnerAccountIdRegExp = /^[-:a-zA-Z0-9]{1,128}$/;

export const UpdateTestCycleBody = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(updateTestCycleBodyKeyRegExp)
      .describe("Unique key of the test cycle"),
    name: zod
      .string()
      .regex(updateTestCycleBodyNameRegExp)
      .describe("Name of the Test Cycle"),
    project: zod
      .object({
        id: zod.number().min(1),
      })
      .describe("The ID of the resource")
      .and(
        zod.object({
          self: zod
            .string()
            .url()
            .optional()
            .describe("The REST API endpoint to get more resource details."),
        }),
      )
      .describe("ID and link relative to Zephyr project."),
    jiraProjectVersion: zod
      .object({
        id: zod.number().min(1),
      })
      .describe("The ID of the resource")
      .and(
        zod.object({
          self: zod
            .string()
            .url()
            .optional()
            .describe("The REST API endpoint to get more resource details."),
        }),
      )
      .nullish()
      .describe(
        "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
      ),
    status: zod
      .object({
        id: zod.number().min(1),
      })
      .describe("The ID of the resource")
      .and(
        zod.object({
          self: zod
            .string()
            .url()
            .optional()
            .describe("The REST API endpoint to get more resource details."),
        }),
      ),
    folder: zod
      .object({
        id: zod.number().min(1),
      })
      .describe("The ID of the resource")
      .and(
        zod.object({
          self: zod
            .string()
            .url()
            .optional()
            .describe("The REST API endpoint to get more resource details."),
        }),
      )
      .nullish(),
    description: zod
      .string()
      .nullish()
      .describe("Description outlining the scope."),
    plannedStartDate: zod
      .string()
      .datetime({})
      .nullish()
      .describe(
        "Planned start date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
      ),
    plannedEndDate: zod
      .string()
      .datetime({})
      .nullish()
      .describe(
        "The planned end date of the test cycle. This field cannot be blank. Setting it as null or excluding it from the request will leave the field values unchanged. ISO 8601 Format (i.e., yyyy-MM-dd'T'HH:mm:ss'Z')",
      ),
    owner: zod
      .object({
        accountId: zod
          .string()
          .regex(updateTestCycleBodyOwnerAccountIdRegExp)
          .nullable()
          .describe("Atlassian Account ID of the Jira user."),
        self: zod
          .string()
          .url()
          .optional()
          .describe(
            "The Jira REST API endpoint to get the full representation of the Jira user.",
          ),
      })
      .nullish(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    links: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .and(
        zod.object({
          issues: zod
            .array(
              zod
                .object({
                  issueId: zod.number().min(1).describe("The Jira issue ID"),
                })
                .and(
                  zod.object({
                    self: zod
                      .string()
                      .url()
                      .optional()
                      .describe(
                        "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                      ),
                    id: zod
                      .number()
                      .min(1)
                      .optional()
                      .describe(
                        "The ID that represents the link between the entity and the Jira issue.",
                      ),
                    target: zod
                      .string()
                      .url()
                      .optional()
                      .describe(
                        "The Jira Cloud REST API endpoint to get the full representation of the issue",
                      ),
                    type: zod
                      .enum(["COVERAGE", "BLOCKS", "RELATED"])
                      .optional()
                      .describe("The link type"),
                  }),
                ),
            )
            .optional()
            .describe("A list of Jira issues linked to this entity"),
          webLinks: zod
            .array(
              zod
                .object({
                  description: zod
                    .string()
                    .optional()
                    .describe("The web link description"),
                  url: zod.string().url().describe("The web link URL"),
                })
                .and(
                  zod.object({
                    self: zod
                      .string()
                      .url()
                      .optional()
                      .describe(
                        "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                      ),
                    id: zod.number().min(1).optional(),
                    type: zod
                      .enum(["COVERAGE", "BLOCKS", "RELATED"])
                      .optional()
                      .describe("The link type"),
                  }),
                ),
            )
            .optional()
            .describe("A list of web links for this entity"),
          testPlans: zod
            .array(
              zod.object({
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the Test Cycle and the Test Plan.",
                  ),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                testPlanId: zod
                  .number()
                  .optional()
                  .describe("The ID of the test plan"),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint to get the full representation of the test plan",
                  ),
              }),
            )
            .optional()
            .describe("A list of test plans linked to a test cycle"),
        }),
      )
      .optional()
      .describe(
        "Represents all links that a Test Cycle has. This property is ignored on update operations.",
      ),
  })
  .describe("Details of a test cycle");

export const UpdateTestCycleDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns links for a test cycle with specified key.
 * @summary Get links
 */
export const getTestCycleLinksPathTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const GetTestCycleLinksParams = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(getTestCycleLinksPathTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const GetTestCycleLinks200Response = zod
  .object({
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
    issues: zod
      .array(
        zod
          .object({
            issueId: zod.number().min(1).describe("The Jira issue ID"),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
              ),
            id: zod
              .number()
              .min(1)
              .optional()
              .describe(
                "The ID that represents the link between the entity and the Jira issue.",
              ),
            target: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Jira Cloud REST API endpoint to get the full representation of the issue",
              ),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
          })
          .strict(),
      )
      .optional()
      .describe("A list of Jira issues linked to this entity"),
    webLinks: zod
      .array(
        zod
          .object({
            description: zod
              .string()
              .optional()
              .describe("The web link description"),
            url: zod.string().url().describe("The web link URL"),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
              ),
            id: zod.number().min(1).optional(),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
          })
          .strict(),
      )
      .optional()
      .describe("A list of web links for this entity"),
    testPlans: zod
      .array(
        zod
          .object({
            id: zod
              .number()
              .min(1)
              .optional()
              .describe(
                "The ID that represents the link between the Test Cycle and the Test Plan.",
              ),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
              ),
            testPlanId: zod
              .number()
              .optional()
              .describe("The ID of the test plan"),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
            target: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint to get the full representation of the test plan",
              ),
          })
          .strict(),
      )
      .optional()
      .describe("A list of test plans linked to a test cycle"),
  })
  .strict()
  .describe(
    "Represents all links that a Test Cycle has. This property is ignored on update operations.",
  );

export const GetTestCycleLinksDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test cycle and a Jira issue.
 * @summary Create issue link
 */
export const createTestCycleIssueLinkPathTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateTestCycleIssueLinkParams = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(createTestCycleIssueLinkPathTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const CreateTestCycleIssueLinkBody = zod.object({
  issueId: zod.number().min(1).describe("The Jira issue ID"),
});

export const CreateTestCycleIssueLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test cycle and a generic URL.
 * @summary Create web link
 */
export const createTestCycleWebLinkPathTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateTestCycleWebLinkParams = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(createTestCycleWebLinkPathTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const CreateTestCycleWebLinkBody = zod.object({
  description: zod.string().optional().describe("The web link description"),
  url: zod.string().url().describe("The web link URL"),
});

export const CreateTestCycleWebLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Retrieves all test plans. Query parameters can be used to filter the results.
 * @summary Get test plans
 */
export const listTestPlansQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const listTestPlansQueryMaxResultsDefault = 10;

export const listTestPlansQueryStartAtDefault = 0;
export const listTestPlansQueryStartAtMin = 0;
export const listTestPlansQueryStartAtMax = 1000000;

export const ListTestPlansQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestPlansQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  maxResults: zod
    .number()
    .min(1)
    .default(listTestPlansQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listTestPlansQueryStartAtMin)
    .max(listTestPlansQueryStartAtMax)
    .default(listTestPlansQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listTestPlans200ResponseOneStartAtMin = 0;

export const listTestPlans200ResponseOneTotalMin = 0;

export const listTestPlans200ResponseTwoValuesItemKeyRegExp = /.+-P[0-9]+/;
export const listTestPlans200ResponseTwoValuesItemNameMax = 255;

export const listTestPlans200ResponseTwoValuesItemNameRegExp = /^(?!\\s*$).+/;

export const listTestPlans200ResponseTwoValuesItemOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const listTestPlans200ResponseTwoValuesItemLabelsMax = 50;

export const ListTestPlans200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listTestPlans200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listTestPlans200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(listTestPlans200ResponseTwoValuesItemKeyRegExp)
              .describe("Key of the test plan"),
            name: zod
              .string()
              .min(1)
              .max(listTestPlans200ResponseTwoValuesItemNameMax)
              .regex(listTestPlans200ResponseTwoValuesItemNameRegExp),
            objective: zod
              .string()
              .nullish()
              .describe("A description of the objective."),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            status: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            folder: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            owner: zod
              .object({
                accountId: zod
                  .string()
                  .regex(
                    listTestPlans200ResponseTwoValuesItemOwnerAccountIdRegExp,
                  )
                  .nullable()
                  .describe("Atlassian Account ID of the Jira user."),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira REST API endpoint to get the full representation of the Jira user.",
                  ),
              })
              .strict()
              .nullish(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            labels: zod
              .array(zod.string())
              .max(listTestPlans200ResponseTwoValuesItemLabelsMax)
              .optional()
              .describe("Array of labels associated to this entity."),
            links: zod
              .object({
                webLinks: zod
                  .array(
                    zod
                      .object({
                        description: zod
                          .string()
                          .optional()
                          .describe("The web link description"),
                        url: zod.string().url().describe("The web link URL"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                          ),
                        id: zod.number().min(1).optional(),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of web links for this entity"),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
                testCycles: zod
                  .array(
                    zod
                      .object({
                        id: zod.number().min(1),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The REST API endpoint to get more resource details.",
                          ),
                        testCycleId: zod.number().min(1).optional(),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                        target: zod
                          .string()
                          .optional()
                          .describe("The test cycle resource"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of test cycle links for a test plan"),
              })
              .strict()
              .optional(),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListTestPlansDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a test plan. All required test plan custom fields should be present in the request.
 * @summary Create test plan
 */
export const createTestPlanBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createTestPlanBodyNameMax = 255;

export const createTestPlanBodyNameRegExp = /^(?!\\s*$).+/;

export const createTestPlanBodyStatusNameMax = 255;

export const createTestPlanBodyOwnerIdRegExp = /^[-:a-zA-Z0-9]{1,128}$/;
export const createTestPlanBodyLabelsMax = 50;

export const CreateTestPlanBody = zod.object({
  projectKey: zod
    .string()
    .regex(createTestPlanBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createTestPlanBodyNameMax)
    .regex(createTestPlanBodyNameRegExp),
  objective: zod.string().nullish().describe("A description of the objective."),
  folderId: zod
    .number()
    .min(1)
    .optional()
    .describe("ID of a folder to place the entity within."),
  statusName: zod
    .string()
    .min(1)
    .max(createTestPlanBodyStatusNameMax)
    .optional()
    .describe("The status name."),
  ownerId: zod
    .string()
    .regex(createTestPlanBodyOwnerIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  labels: zod
    .array(zod.string())
    .max(createTestPlanBodyLabelsMax)
    .optional()
    .describe("Array of labels associated to this entity."),
  customFields: zod
    .record(zod.string(), zod.unknown())
    .optional()
    .describe(
      "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
    ),
});

export const CreateTestPlan201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
    key: zod.string().optional(),
  })
  .strict();

export const CreateTestPlanDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a test plan for the given id or key.
 * @summary Get test plan
 */
export const getTestPlanPathTestPlanIdOrKeyRegExp = /([0-9]+)|(.+-P[0-9]+)/;

export const GetTestPlanParams = zod.object({
  testPlanIdOrKey: zod
    .string()
    .regex(getTestPlanPathTestPlanIdOrKeyRegExp)
    .describe(
      "The ID or key of the test plan. Test plan keys are of the format [A-Z]+-P[0-9]+",
    ),
});

export const getTestPlan200ResponseKeyRegExp = /.+-P[0-9]+/;
export const getTestPlan200ResponseNameMax = 255;

export const getTestPlan200ResponseNameRegExp = /^(?!\\s*$).+/;

export const getTestPlan200ResponseOwnerAccountIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const getTestPlan200ResponseLabelsMax = 50;

export const GetTestPlan200Response = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(getTestPlan200ResponseKeyRegExp)
      .describe("Key of the test plan"),
    name: zod
      .string()
      .min(1)
      .max(getTestPlan200ResponseNameMax)
      .regex(getTestPlan200ResponseNameRegExp),
    objective: zod
      .string()
      .nullish()
      .describe("A description of the objective."),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    status: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    folder: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    owner: zod
      .object({
        accountId: zod
          .string()
          .regex(getTestPlan200ResponseOwnerAccountIdRegExp)
          .nullable()
          .describe("Atlassian Account ID of the Jira user."),
        self: zod
          .string()
          .url()
          .optional()
          .describe(
            "The Jira REST API endpoint to get the full representation of the Jira user.",
          ),
      })
      .strict()
      .nullish(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    labels: zod
      .array(zod.string())
      .max(getTestPlan200ResponseLabelsMax)
      .optional()
      .describe("Array of labels associated to this entity."),
    links: zod
      .object({
        webLinks: zod
          .array(
            zod
              .object({
                description: zod
                  .string()
                  .optional()
                  .describe("The web link description"),
                url: zod.string().url().describe("The web link URL"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and this web link.",
                  ),
                id: zod.number().min(1).optional(),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of web links for this entity"),
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the entity and the Jira issue.",
                  ),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira Cloud REST API endpoint to get the full representation of the issue",
                  ),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
        testCycles: zod
          .array(
            zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                testCycleId: zod.number().min(1).optional(),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
                target: zod
                  .string()
                  .optional()
                  .describe("The test cycle resource"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of test cycle links for a test plan"),
      })
      .strict()
      .optional(),
  })
  .strict();

export const GetTestPlanDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test plan and a generic URL.
 * @summary Create web link
 */
export const createTestPlanWebLinkPathTestPlanIdOrKeyRegExp =
  /([0-9]+)|(.+-P[0-9]+)/;

export const CreateTestPlanWebLinkParams = zod.object({
  testPlanIdOrKey: zod
    .string()
    .regex(createTestPlanWebLinkPathTestPlanIdOrKeyRegExp)
    .describe(
      "The ID or key of the test plan. Test plan keys are of the format [A-Z]+-P[0-9]+",
    ),
});

export const CreateTestPlanWebLinkBody = zod
  .object({
    description: zod.string().optional().describe("The web link description"),
    url: zod.string().url().describe("The web link URL"),
  })
  .and(zod.unknown());

export const CreateTestPlanWebLink201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestPlanWebLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test plan and a Jira issue.
 * @summary Create issue link
 */
export const createTestPlanIssueLinkPathTestPlanIdOrKeyRegExp =
  /([0-9]+)|(.+-P[0-9]+)/;

export const CreateTestPlanIssueLinkParams = zod.object({
  testPlanIdOrKey: zod
    .string()
    .regex(createTestPlanIssueLinkPathTestPlanIdOrKeyRegExp)
    .describe(
      "The ID or key of the test plan. Test plan keys are of the format [A-Z]+-P[0-9]+",
    ),
});

export const CreateTestPlanIssueLinkBody = zod.object({
  issueId: zod.number().min(1).describe("The Jira issue ID"),
});

export const CreateTestPlanIssueLink201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestPlanIssueLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test plan and a test cycle.
 * @summary Create test cycle link
 */
export const createTestPlanTestCycleLinkPathTestPlanIdOrKeyRegExp =
  /([0-9]+)|(.+-P[0-9]+)/;

export const CreateTestPlanTestCycleLinkParams = zod.object({
  testPlanIdOrKey: zod
    .string()
    .regex(createTestPlanTestCycleLinkPathTestPlanIdOrKeyRegExp)
    .describe(
      "The ID or key of the test plan. Test plan keys are of the format [A-Z]+-P[0-9]+",
    ),
});

export const createTestPlanTestCycleLinkBodyTestCycleIdOrKeyRegExp =
  /([0-9]+)|([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateTestPlanTestCycleLinkBody = zod.object({
  testCycleIdOrKey: zod
    .string()
    .regex(createTestPlanTestCycleLinkBodyTestCycleIdOrKeyRegExp)
    .describe("The ID or key of the test cycle."),
});

export const CreateTestPlanTestCycleLink201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestPlanTestCycleLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all test executions. Query parameters can be used to filter by project and folder.
 * @summary Get test executions
 */
export const listTestExecutionsQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const listTestExecutionsQueryTestCycleRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const listTestExecutionsQueryTestCaseRegExp = /([0-9]+)|(.+-T[0-9]+)/;
export const listTestExecutionsQueryIncludeStepLinksDefault = false;
export const listTestExecutionsQueryOnlyLastExecutionsDefault = false;
export const listTestExecutionsQueryMaxResultsDefault = 10;

export const listTestExecutionsQueryStartAtDefault = 0;
export const listTestExecutionsQueryStartAtMin = 0;
export const listTestExecutionsQueryStartAtMax = 1000000;

export const ListTestExecutionsQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestExecutionsQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  testCycle: zod
    .string()
    .regex(listTestExecutionsQueryTestCycleRegExp)
    .optional()
    .describe("Test cycle key filter."),
  testCase: zod
    .string()
    .regex(listTestExecutionsQueryTestCaseRegExp)
    .optional()
    .describe("Test case key filter."),
  actualEndDateAfter: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Filter for 'Actual End Date' after the given time. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  actualEndDateBefore: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Filter for 'Actual End Date' before the given time. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  includeStepLinks: zod
    .boolean()
    .default(listTestExecutionsQueryIncludeStepLinksDefault)
    .describe(
      "If true, execution step issue links will be included in the response",
    ),
  jiraProjectVersionId: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
    ),
  onlyLastExecutions: zod
    .boolean()
    .default(listTestExecutionsQueryOnlyLastExecutionsDefault)
    .describe(
      "If true, includes only the last execution of each test cycle item (test case), and all ad-hoc test executions.",
    ),
  maxResults: zod
    .number()
    .min(1)
    .default(listTestExecutionsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listTestExecutionsQueryStartAtMin)
    .max(listTestExecutionsQueryStartAtMax)
    .default(listTestExecutionsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listTestExecutions200ResponseOneStartAtMin = 0;

export const listTestExecutions200ResponseOneTotalMin = 0;

export const listTestExecutions200ResponseTwoValuesItemKeyRegExp = /.+-E[0-9]+/;

export const listTestExecutions200ResponseTwoValuesItemEstimatedTimeMin = 0;

export const listTestExecutions200ResponseTwoValuesItemExecutionTimeMin = 0;

export const listTestExecutions200ResponseTwoValuesItemExecutedByIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const listTestExecutions200ResponseTwoValuesItemAssignedToIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const ListTestExecutions200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listTestExecutions200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listTestExecutions200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(listTestExecutions200ResponseTwoValuesItemKeyRegExp)
              .optional()
              .describe("Test execution key"),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            testCase: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                id: zod.number().min(1).optional(),
              })
              .strict(),
            environment: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            jiraProjectVersion: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish()
              .describe(
                "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
              ),
            testExecutionStatus: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            actualEndDate: zod
              .string()
              .datetime({})
              .optional()
              .describe(
                "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
              ),
            estimatedTime: zod
              .number()
              .min(listTestExecutions200ResponseTwoValuesItemEstimatedTimeMin)
              .nullish()
              .describe("Estimated duration in milliseconds."),
            executionTime: zod
              .number()
              .min(listTestExecutions200ResponseTwoValuesItemExecutionTimeMin)
              .nullish()
              .describe("Actual test execution time in milliseconds."),
            executedById: zod
              .string()
              .regex(
                listTestExecutions200ResponseTwoValuesItemExecutedByIdRegExp,
              )
              .nullish()
              .describe("Atlassian Account ID of the Jira user."),
            assignedToId: zod
              .string()
              .regex(
                listTestExecutions200ResponseTwoValuesItemAssignedToIdRegExp,
              )
              .nullish()
              .describe("Atlassian Account ID of the Jira user."),
            comment: zod
              .string()
              .nullish()
              .describe("Comment added against overall test case execution."),
            automated: zod
              .boolean()
              .optional()
              .describe(
                "Indicates if the test execution was done manually or not.",
              ),
            testCycle: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                id: zod.number().min(1).optional(),
              })
              .strict()
              .nullish(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            links: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
              })
              .strict()
              .optional(),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListTestExecutionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a test execution. All required test execution custom fields should be present in the request.
 * @summary Create test execution
 */
export const createTestExecutionBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createTestExecutionBodyTestCaseKeyRegExp = /(.+-T[0-9]+)/;
export const createTestExecutionBodyTestCycleKeyRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const createTestExecutionBodyStatusNameMax = 255;

export const createTestExecutionBodyTestScriptResultsItemStatusNameMax = 255;

export const createTestExecutionBodyExecutionTimeMin = 0;

export const createTestExecutionBodyExecutedByIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const createTestExecutionBodyAssignedToIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const CreateTestExecutionBody = zod.object({
  projectKey: zod
    .string()
    .regex(createTestExecutionBodyProjectKeyRegExp)
    .describe("Jira project key."),
  testCaseKey: zod
    .string()
    .regex(createTestExecutionBodyTestCaseKeyRegExp)
    .describe(
      "Key of test case the execution applies to. NOTE: Test cases with call to test, parameters and test data are not supported.",
    ),
  testCycleKey: zod
    .string()
    .regex(createTestExecutionBodyTestCycleKeyRegExp)
    .describe("Key of test cycle the execution applies to."),
  statusName: zod
    .string()
    .min(1)
    .max(createTestExecutionBodyStatusNameMax)
    .describe("The status name."),
  testScriptResults: zod
    .array(
      zod.object({
        statusName: zod
          .string()
          .min(1)
          .max(createTestExecutionBodyTestScriptResultsItemStatusNameMax)
          .describe("The status name."),
        actualEndDate: zod
          .string()
          .datetime({})
          .optional()
          .describe(
            "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
          ),
        actualResult: zod
          .string()
          .optional()
          .describe(
            "free text field to provide more info on result of step execution.",
          ),
      }),
    )
    .optional(),
  environmentName: zod
    .string()
    .optional()
    .describe("Environment assigned to the test case."),
  actualEndDate: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  executionTime: zod
    .number()
    .min(createTestExecutionBodyExecutionTimeMin)
    .optional()
    .describe("Actual test execution time in milliseconds."),
  executedById: zod
    .string()
    .regex(createTestExecutionBodyExecutedByIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  assignedToId: zod
    .string()
    .regex(createTestExecutionBodyAssignedToIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  comment: zod
    .string()
    .nullish()
    .describe("Comment added against overall test case execution."),
  customFields: zod
    .record(zod.string(), zod.unknown())
    .optional()
    .describe(
      "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
    ),
});

export const CreateTestExecution201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateTestExecutionDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Retrieves all test executions. Query parameters can be used to filter the results.

Use this endpoint for retrieving large volumes of test executions.

Results are split into separate pages, each of the same size as the `limit` query parameter.
To fetch the next page, use the `next` URL returned in the response.
It should have the same parameters as the original request, with the updated value for `startAtId` parameter.
Alternatively, the same request can be made with the `startAtId` parameter set to the `nextStartId` value
returned in the response.

 * @summary Get test executions (NextGen)
 */
export const listTestExecutionsNextgenQueryProjectKeyRegExp =
  /([A-Z][A-Z_0-9]+)/;
export const listTestExecutionsNextgenQueryTestCycleRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;
export const listTestExecutionsNextgenQueryTestCaseRegExp =
  /([0-9]+)|(.+-T[0-9]+)/;
export const listTestExecutionsNextgenQueryIncludeStepLinksDefault = false;
export const listTestExecutionsNextgenQueryOnlyLastExecutionsDefault = false;
export const listTestExecutionsNextgenQueryLimitDefault = 10;
export const listTestExecutionsNextgenQueryLimitMax = 1000;

export const listTestExecutionsNextgenQueryStartAtIdDefault = 0;
export const listTestExecutionsNextgenQueryStartAtIdMin = 0;

export const ListTestExecutionsNextgenQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listTestExecutionsNextgenQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  testCycle: zod
    .string()
    .regex(listTestExecutionsNextgenQueryTestCycleRegExp)
    .optional()
    .describe("Test cycle key filter."),
  testCase: zod
    .string()
    .regex(listTestExecutionsNextgenQueryTestCaseRegExp)
    .optional()
    .describe("Test case key filter."),
  actualEndDateAfter: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Filter for 'Actual End Date' after the given time. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  actualEndDateBefore: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "Filter for 'Actual End Date' before the given time. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  includeStepLinks: zod
    .boolean()
    .default(listTestExecutionsNextgenQueryIncludeStepLinksDefault)
    .describe(
      "If true, execution step issue links will be included in the response",
    ),
  jiraProjectVersionId: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
    ),
  onlyLastExecutions: zod
    .boolean()
    .default(listTestExecutionsNextgenQueryOnlyLastExecutionsDefault)
    .describe(
      "If true, includes only the last execution of each test cycle item (test case), and all ad-hoc test executions.",
    ),
  limit: zod
    .number()
    .min(1)
    .max(listTestExecutionsNextgenQueryLimitMax)
    .default(listTestExecutionsNextgenQueryLimitDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the limit value in the response to confirm how many results were actually returned.\n",
    ),
  startAtId: zod
    .number()
    .min(listTestExecutionsNextgenQueryStartAtIdMin)
    .default(listTestExecutionsNextgenQueryStartAtIdDefault)
    .describe("Zero-indexed starting position for ID-based pagination."),
});

export const listTestExecutionsNextgen200ResponseOneNextStartAtIdMin = 0;

export const listTestExecutionsNextgen200ResponseOneLimitMin = 0;

export const listTestExecutionsNextgen200ResponseTwoValuesItemKeyRegExp =
  /.+-E[0-9]+/;

export const listTestExecutionsNextgen200ResponseTwoValuesItemEstimatedTimeMin = 0;

export const listTestExecutionsNextgen200ResponseTwoValuesItemExecutionTimeMin = 0;

export const listTestExecutionsNextgen200ResponseTwoValuesItemExecutedByIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const listTestExecutionsNextgen200ResponseTwoValuesItemAssignedToIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const ListTestExecutionsNextgen200Response = zod
  .object({
    next: zod.string().url().nullish(),
    nextStartAtId: zod
      .number()
      .min(listTestExecutionsNextgen200ResponseOneNextStartAtIdMin)
      .nullable(),
    limit: zod.number().min(listTestExecutionsNextgen200ResponseOneLimitMin),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            key: zod
              .string()
              .regex(listTestExecutionsNextgen200ResponseTwoValuesItemKeyRegExp)
              .optional()
              .describe("Test execution key"),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            testCase: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                id: zod.number().min(1).optional(),
              })
              .strict(),
            environment: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish(),
            jiraProjectVersion: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .nullish()
              .describe(
                "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
              ),
            testExecutionStatus: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict(),
            actualEndDate: zod
              .string()
              .datetime({})
              .optional()
              .describe(
                "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
              ),
            estimatedTime: zod
              .number()
              .min(
                listTestExecutionsNextgen200ResponseTwoValuesItemEstimatedTimeMin,
              )
              .nullish()
              .describe("Estimated duration in milliseconds."),
            executionTime: zod
              .number()
              .min(
                listTestExecutionsNextgen200ResponseTwoValuesItemExecutionTimeMin,
              )
              .nullish()
              .describe("Actual test execution time in milliseconds."),
            executedById: zod
              .string()
              .regex(
                listTestExecutionsNextgen200ResponseTwoValuesItemExecutedByIdRegExp,
              )
              .nullish()
              .describe("Atlassian Account ID of the Jira user."),
            assignedToId: zod
              .string()
              .regex(
                listTestExecutionsNextgen200ResponseTwoValuesItemAssignedToIdRegExp,
              )
              .nullish()
              .describe("Atlassian Account ID of the Jira user."),
            comment: zod
              .string()
              .nullish()
              .describe("Comment added against overall test case execution."),
            automated: zod
              .boolean()
              .optional()
              .describe(
                "Indicates if the test execution was done manually or not.",
              ),
            testCycle: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                id: zod.number().min(1).optional(),
              })
              .strict()
              .nullish(),
            customFields: zod
              .record(zod.string(), zod.unknown())
              .optional()
              .describe(
                "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
              ),
            links: zod
              .object({
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
                issues: zod
                  .array(
                    zod
                      .object({
                        issueId: zod
                          .number()
                          .min(1)
                          .describe("The Jira issue ID"),
                        self: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                          ),
                        id: zod
                          .number()
                          .min(1)
                          .optional()
                          .describe(
                            "The ID that represents the link between the entity and the Jira issue.",
                          ),
                        target: zod
                          .string()
                          .url()
                          .optional()
                          .describe(
                            "The Jira Cloud REST API endpoint to get the full representation of the issue",
                          ),
                        type: zod
                          .enum(["COVERAGE", "BLOCKS", "RELATED"])
                          .optional()
                          .describe("The link type"),
                      })
                      .strict(),
                  )
                  .optional()
                  .describe("A list of Jira issues linked to this entity"),
              })
              .strict()
              .optional(),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListTestExecutionsNextgenDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a test execution for the given ID.
 * @summary Get test execution
 */
export const getTestExecutionPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const GetTestExecutionParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(getTestExecutionPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const getTestExecutionQueryIncludeStepLinksDefault = false;

export const GetTestExecutionQueryParams = zod.object({
  includeStepLinks: zod
    .boolean()
    .default(getTestExecutionQueryIncludeStepLinksDefault)
    .describe(
      "If true, execution step issue links will be included in the response",
    ),
});

export const getTestExecution200ResponseKeyRegExp = /.+-E[0-9]+/;

export const getTestExecution200ResponseEstimatedTimeMin = 0;

export const getTestExecution200ResponseExecutionTimeMin = 0;

export const getTestExecution200ResponseExecutedByIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const getTestExecution200ResponseAssignedToIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const GetTestExecution200Response = zod
  .object({
    id: zod.number().min(1),
    key: zod
      .string()
      .regex(getTestExecution200ResponseKeyRegExp)
      .optional()
      .describe("Test execution key"),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    testCase: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        id: zod.number().min(1).optional(),
      })
      .strict(),
    environment: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish(),
    jiraProjectVersion: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .nullish()
      .describe(
        "ID and Link to fetch information about Jira Project version. Relates to 'Version' or 'Releases' in Jira projects.",
      ),
    testExecutionStatus: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict(),
    actualEndDate: zod
      .string()
      .datetime({})
      .optional()
      .describe(
        "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
      ),
    estimatedTime: zod
      .number()
      .min(getTestExecution200ResponseEstimatedTimeMin)
      .nullish()
      .describe("Estimated duration in milliseconds."),
    executionTime: zod
      .number()
      .min(getTestExecution200ResponseExecutionTimeMin)
      .nullish()
      .describe("Actual test execution time in milliseconds."),
    executedById: zod
      .string()
      .regex(getTestExecution200ResponseExecutedByIdRegExp)
      .nullish()
      .describe("Atlassian Account ID of the Jira user."),
    assignedToId: zod
      .string()
      .regex(getTestExecution200ResponseAssignedToIdRegExp)
      .nullish()
      .describe("Atlassian Account ID of the Jira user."),
    comment: zod
      .string()
      .nullish()
      .describe("Comment added against overall test case execution."),
    automated: zod
      .boolean()
      .optional()
      .describe("Indicates if the test execution was done manually or not."),
    testCycle: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        id: zod.number().min(1).optional(),
      })
      .strict()
      .nullish(),
    customFields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
      ),
    links: zod
      .object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
        issues: zod
          .array(
            zod
              .object({
                issueId: zod.number().min(1).describe("The Jira issue ID"),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
                  ),
                id: zod
                  .number()
                  .min(1)
                  .optional()
                  .describe(
                    "The ID that represents the link between the entity and the Jira issue.",
                  ),
                target: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The Jira Cloud REST API endpoint to get the full representation of the issue",
                  ),
                type: zod
                  .enum(["COVERAGE", "BLOCKS", "RELATED"])
                  .optional()
                  .describe("The link type"),
              })
              .strict(),
          )
          .optional()
          .describe("A list of Jira issues linked to this entity"),
      })
      .strict()
      .optional(),
  })
  .strict();

export const GetTestExecutionDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Update the test execution. This operation only updates specified fields in the payload and ignores `null` or `undefined` values.
 * @summary Update test execution
 */
export const updateTestExecutionPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const UpdateTestExecutionParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(updateTestExecutionPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const updateTestExecutionBodyStatusNameMax = 255;

export const updateTestExecutionBodyExecutionTimeMin = 0;

export const updateTestExecutionBodyExecutedByIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;
export const updateTestExecutionBodyAssignedToIdRegExp =
  /^[-:a-zA-Z0-9]{1,128}$/;

export const UpdateTestExecutionBody = zod.object({
  statusName: zod
    .string()
    .min(1)
    .max(updateTestExecutionBodyStatusNameMax)
    .optional()
    .describe("The status name."),
  environmentName: zod
    .string()
    .optional()
    .describe("Environment assigned to the test case."),
  actualEndDate: zod
    .string()
    .datetime({})
    .optional()
    .describe(
      "The actual end date of the test cycle. Format: yyyy-MM-dd'T'HH:mm:ss'Z'",
    ),
  executionTime: zod
    .number()
    .min(updateTestExecutionBodyExecutionTimeMin)
    .optional()
    .describe("Actual test execution time in milliseconds."),
  executedById: zod
    .string()
    .regex(updateTestExecutionBodyExecutedByIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  assignedToId: zod
    .string()
    .regex(updateTestExecutionBodyAssignedToIdRegExp)
    .nullish()
    .describe("Atlassian Account ID of the Jira user."),
  comment: zod
    .string()
    .nullish()
    .describe("Comment added against overall test case execution."),
});

export const UpdateTestExecutionDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns the test steps for the given test execution. Provides a paged response.
 * @summary Get test steps
 */
export const getTestExecutionTestStepsPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const GetTestExecutionTestStepsParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(getTestExecutionTestStepsPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const getTestExecutionTestStepsQueryMaxResultsDefault = 10;

export const getTestExecutionTestStepsQueryStartAtDefault = 0;
export const getTestExecutionTestStepsQueryStartAtMin = 0;
export const getTestExecutionTestStepsQueryStartAtMax = 1000000;

export const getTestExecutionTestStepsQueryTestDataRowNumberMin = 0;

export const GetTestExecutionTestStepsQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(getTestExecutionTestStepsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(getTestExecutionTestStepsQueryStartAtMin)
    .max(getTestExecutionTestStepsQueryStartAtMax)
    .default(getTestExecutionTestStepsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
  testDataRowNumber: zod
    .number()
    .min(getTestExecutionTestStepsQueryTestDataRowNumberMin)
    .optional()
    .describe("The id of the test data row to retrieve."),
});

export const getTestExecutionTestSteps200ResponseOneStartAtMin = 0;

export const getTestExecutionTestSteps200ResponseOneTotalMin = 0;

export const GetTestExecutionTestSteps200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(getTestExecutionTestSteps200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(getTestExecutionTestSteps200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            inline: zod
              .object({
                description: zod
                  .string()
                  .nullish()
                  .describe("The instruction to be followed"),
                testData: zod
                  .string()
                  .nullish()
                  .describe(
                    "Any test data required to perform the instruction (optional). The fields values provided can be interpolated into the description.",
                  ),
                expectedResult: zod
                  .string()
                  .nullish()
                  .describe(
                    "The expected outcome of executing the instruction",
                  ),
                actualResult: zod
                  .string()
                  .nullish()
                  .describe("The actual outcome of executing the instruction"),
                testDataRowNumber: zod
                  .number()
                  .nullish()
                  .describe("The id of the test data row"),
                reflectRef: zod
                  .string()
                  .nullish()
                  .describe("The AI reference. Zephyr only feature"),
                status: zod
                  .object({
                    id: zod.number().min(1),
                    self: zod
                      .string()
                      .url()
                      .optional()
                      .describe(
                        "The REST API endpoint to get more resource details.",
                      ),
                  })
                  .strict()
                  .optional(),
              })
              .strict()
              .optional(),
          })
          .strict()
          .describe("Inline test step"),
      )
      .optional()
      .describe("The list of test steps"),
  })
  .strict()
  .describe("Response body when retrieving test steps for a test execution");

/**
 * Updates the test steps for the given test execution.
 * @summary Update test steps
 */
export const putTestExecutionTestStepsPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const PutTestExecutionTestStepsParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(putTestExecutionTestStepsPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const putTestExecutionTestStepsBodyStepsItemStatusNameMax = 255;

export const PutTestExecutionTestStepsBody = zod.object({
  steps: zod
    .array(
      zod.object({
        actualResult: zod.string().optional().describe("The actual result."),
        statusName: zod
          .string()
          .min(1)
          .max(putTestExecutionTestStepsBodyStepsItemStatusNameMax)
          .optional()
          .describe("The status name."),
      }),
    )
    .optional(),
});

export const PutTestExecutionTestSteps404Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const PutTestExecutionTestSteps409Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const PutTestExecutionTestSteps422Response = zod.union([
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
]);

export const PutTestExecutionTestStepsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Sync test execution with content of test case script
 * @summary Sync test execution
 */
export const syncTestExecutionScriptPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const SyncTestExecutionScriptParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(syncTestExecutionScriptPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const SyncTestExecutionScript200Response = zod
  .object({
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
  })
  .strict();

export const SyncTestExecutionScript404Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const SyncTestExecutionScript409Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const SyncTestExecutionScriptDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns links for a test execution with specified ID.
 * @summary Get links
 */
export const listTestExecutionLinksPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const ListTestExecutionLinksParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(listTestExecutionLinksPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const ListTestExecutionLinks200Response = zod
  .object({
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
    issues: zod
      .array(
        zod
          .object({
            issueId: zod.number().min(1).describe("The Jira issue ID"),
            self: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Zephyr REST API endpoint relative to the link between the entity and the Jira issue.",
              ),
            id: zod
              .number()
              .min(1)
              .optional()
              .describe(
                "The ID that represents the link between the entity and the Jira issue.",
              ),
            target: zod
              .string()
              .url()
              .optional()
              .describe(
                "The Jira Cloud REST API endpoint to get the full representation of the issue",
              ),
            type: zod
              .enum(["COVERAGE", "BLOCKS", "RELATED"])
              .optional()
              .describe("The link type"),
          })
          .strict(),
      )
      .optional()
      .describe("A list of Jira issues linked to this entity"),
  })
  .strict();

export const ListTestExecutionLinksDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a link between a test execution and a Jira issue.
 * @summary Create issue link
 */
export const createTestExecutionIssueLinkPathTestExecutionIdOrKeyRegExp =
  /([0-9]+)|(.+-E[0-9]+)/;

export const CreateTestExecutionIssueLinkParams = zod.object({
  testExecutionIdOrKey: zod
    .string()
    .regex(createTestExecutionIssueLinkPathTestExecutionIdOrKeyRegExp)
    .describe(
      "The ID or key of the test execution. Test execution keys are of the format [A-Z]+-E[0-9]+",
    ),
});

export const CreateTestExecutionIssueLinkBody = zod.object({
  issueId: zod.number().min(1).describe("The Jira issue ID"),
});

export const CreateTestExecutionIssueLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all projects.
 * @summary Get projects
 */
export const listProjectsQueryMaxResultsDefault = 10;

export const listProjectsQueryStartAtDefault = 0;
export const listProjectsQueryStartAtMin = 0;
export const listProjectsQueryStartAtMax = 1000000;

export const ListProjectsQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(listProjectsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listProjectsQueryStartAtMin)
    .max(listProjectsQueryStartAtMax)
    .default(listProjectsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listProjects200ResponseOneStartAtMin = 0;

export const listProjects200ResponseOneTotalMin = 0;

export const ListProjects200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listProjects200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listProjects200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().describe("The ID of the project in Zephyr."),
            jiraProjectId: zod
              .number()
              .describe("The ID of the project in Jira."),
            key: zod.string(),
            enabled: zod
              .boolean()
              .describe(
                "Indicates whether the project has Zephyr enabled on it.",
              ),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListProjectsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a project for the given ID or key.
 * @summary Get project
 */
export const getProjectPathProjectIdOrKeyRegExp = /([0-9]+)|([A-Z][A-Z_0-9]+)/;

export const GetProjectParams = zod.object({
  projectIdOrKey: zod
    .string()
    .regex(getProjectPathProjectIdOrKeyRegExp)
    .describe("The Zephyr project ID or Jira project key"),
});

export const GetProject200Response = zod
  .object({
    id: zod.number().describe("The ID of the project in Zephyr."),
    jiraProjectId: zod.number().describe("The ID of the project in Jira."),
    key: zod.string(),
    enabled: zod
      .boolean()
      .describe("Indicates whether the project has Zephyr enabled on it."),
  })
  .strict();

export const GetProjectDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all folder.
 * @summary Get folders
 */
export const listFoldersQueryMaxResultsDefault = 10;

export const listFoldersQueryStartAtDefault = 0;
export const listFoldersQueryStartAtMin = 0;
export const listFoldersQueryStartAtMax = 1000000;

export const listFoldersQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const ListFoldersQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(listFoldersQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listFoldersQueryStartAtMin)
    .max(listFoldersQueryStartAtMax)
    .default(listFoldersQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
  projectKey: zod
    .string()
    .regex(listFoldersQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  folderType: zod
    .string()
    .optional()
    .describe(
      'Folder type filter. Either `"TEST_CASE"`, `"TEST_PLAN"` or `"TEST_CYCLE"`',
    ),
});

export const listFolders200ResponseOneStartAtMin = 0;

export const listFolders200ResponseOneTotalMin = 0;

export const listFolders200ResponseTwoValuesItemNameMax = 255;

export const listFolders200ResponseTwoValuesItemNameRegExp = /^(?!\\s*$).+/;
export const listFolders200ResponseTwoValuesItemIndexMin = 0;

export const ListFolders200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listFolders200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listFolders200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            parentId: zod.number().min(1),
            name: zod
              .string()
              .min(1)
              .max(listFolders200ResponseTwoValuesItemNameMax)
              .regex(listFolders200ResponseTwoValuesItemNameRegExp),
            index: zod
              .number()
              .min(listFolders200ResponseTwoValuesItemIndexMin),
            folderType: zod
              .string()
              .describe(
                'Valid values: `"TEST_CASE"`, `"TEST_PLAN"`, `"TEST_CYCLE"`',
              ),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .optional()
              .describe("ID and link relative to Zephyr project."),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListFoldersDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a folder.
 * @summary Create folder
 */

export const createFolderBodyNameMax = 255;

export const createFolderBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const CreateFolderBody = zod.object({
  parentId: zod
    .number()
    .min(1)
    .optional()
    .describe(
      "Folder ID of the parent folder. Must be `null` for root folders.",
    ),
  name: zod
    .string()
    .min(1)
    .max(createFolderBodyNameMax)
    .describe(
      "Folder name. Folder name must not contain `/` and `\\` characters.",
    ),
  projectKey: zod
    .string()
    .regex(createFolderBodyProjectKeyRegExp)
    .describe("Jira project key."),
  folderType: zod
    .string()
    .describe('Valid values: `"TEST_CASE"`, `"TEST_PLAN"`, `"TEST_CYCLE"`'),
});

export const CreateFolder201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateFolderDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a folder for the given ID.
 * @summary Get folder
 */

export const GetFolderParams = zod.object({
  folderId: zod.number().min(1).describe("Folder ID"),
});

export const getFolder200ResponseNameMax = 255;

export const getFolder200ResponseNameRegExp = /^(?!\\s*$).+/;
export const getFolder200ResponseIndexMin = 0;

export const GetFolder200Response = zod
  .object({
    id: zod.number().min(1),
    parentId: zod.number().min(1),
    name: zod
      .string()
      .min(1)
      .max(getFolder200ResponseNameMax)
      .regex(getFolder200ResponseNameRegExp),
    index: zod.number().min(getFolder200ResponseIndexMin),
    folderType: zod
      .string()
      .describe('Valid values: `"TEST_CASE"`, `"TEST_PLAN"`, `"TEST_CYCLE"`'),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .optional()
      .describe("ID and link relative to Zephyr project."),
  })
  .strict();

export const GetFolderDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all priorities.
 * @summary Get priorities
 */
export const listPrioritiesQueryMaxResultsDefault = 10;

export const listPrioritiesQueryStartAtDefault = 0;
export const listPrioritiesQueryStartAtMin = 0;
export const listPrioritiesQueryStartAtMax = 1000000;

export const listPrioritiesQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const ListPrioritiesQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(listPrioritiesQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listPrioritiesQueryStartAtMin)
    .max(listPrioritiesQueryStartAtMax)
    .default(listPrioritiesQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
  projectKey: zod
    .string()
    .regex(listPrioritiesQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
});

export const listPriorities200ResponseOneStartAtMin = 0;

export const listPriorities200ResponseOneTotalMin = 0;

export const listPriorities200ResponseTwoValuesItemOneNameMax = 255;

export const listPriorities200ResponseTwoValuesItemOneNameRegExp =
  /^(?!\\s*$).+/;
export const listPriorities200ResponseTwoValuesItemOneIndexMin = 0;

export const listPriorities200ResponseTwoValuesItemTwoColorRegExp =
  /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;
export const listPriorities200ResponseTwoValuesItemTwoDefaultDefault = false;

export const ListPriorities200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listPriorities200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listPriorities200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            name: zod
              .string()
              .min(1)
              .max(listPriorities200ResponseTwoValuesItemOneNameMax)
              .regex(listPriorities200ResponseTwoValuesItemOneNameRegExp),
            description: zod
              .string()
              .nullish()
              .describe("Description outlining the scope."),
            index: zod
              .number()
              .min(listPriorities200ResponseTwoValuesItemOneIndexMin),
            color: zod
              .string()
              .regex(listPriorities200ResponseTwoValuesItemTwoColorRegExp)
              .optional()
              .describe("A color in hexadecimal format"),
            default: zod
              .boolean()
              .default(listPriorities200ResponseTwoValuesItemTwoDefaultDefault),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListPrioritiesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a priority.
 * @summary Create a priority
 */
export const createPriorityBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createPriorityBodyNameMax = 255;

export const createPriorityBodyDescriptionMax = 255;

export const createPriorityBodyColorRegExp = /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;

export const CreatePriorityBody = zod.object({
  projectKey: zod
    .string()
    .regex(createPriorityBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createPriorityBodyNameMax)
    .describe("The priority name."),
  description: zod
    .string()
    .min(1)
    .max(createPriorityBodyDescriptionMax)
    .optional()
    .describe("The priority description."),
  color: zod
    .string()
    .regex(createPriorityBodyColorRegExp)
    .optional()
    .describe("A color in hexadecimal format"),
});

export const CreatePriority201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreatePriorityDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a priority for the given ID.
 * @summary Get priority
 */

export const GetPriorityParams = zod.object({
  priorityId: zod.number().min(1),
});

export const getPriority200ResponseOneNameMax = 255;

export const getPriority200ResponseOneNameRegExp = /^(?!\\s*$).+/;
export const getPriority200ResponseOneIndexMin = 0;

export const getPriority200ResponseTwoColorRegExp =
  /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;
export const getPriority200ResponseTwoDefaultDefault = false;

export const GetPriority200Response = zod
  .object({
    id: zod.number().min(1),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    name: zod
      .string()
      .min(1)
      .max(getPriority200ResponseOneNameMax)
      .regex(getPriority200ResponseOneNameRegExp),
    description: zod
      .string()
      .nullish()
      .describe("Description outlining the scope."),
    index: zod.number().min(getPriority200ResponseOneIndexMin),
    color: zod
      .string()
      .regex(getPriority200ResponseTwoColorRegExp)
      .optional()
      .describe("A color in hexadecimal format"),
    default: zod.boolean().default(getPriority200ResponseTwoDefaultDefault),
  })
  .strict();

export const GetPriority404Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const GetPriorityDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Update an existing priority.  Please take into account that for each non-specified field the value will be cleared.

 * @summary Update priority
 */

export const UpdatePriorityParams = zod.object({
  priorityId: zod.number().min(1),
});

export const updatePriorityBodyNameMax = 255;

export const updatePriorityBodyDescriptionMax = 255;

export const updatePriorityBodyIndexMin = 0;

export const updatePriorityBodyColorRegExp = /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;

export const UpdatePriorityBody = zod.object({
  id: zod.number().min(1),
  project: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .describe("ID and link relative to Zephyr project."),
  name: zod
    .string()
    .min(1)
    .max(updatePriorityBodyNameMax)
    .describe("The priority name."),
  description: zod
    .string()
    .min(1)
    .max(updatePriorityBodyDescriptionMax)
    .optional()
    .describe("The priority description."),
  index: zod.number().min(updatePriorityBodyIndexMin),
  default: zod.boolean(),
  color: zod
    .string()
    .regex(updatePriorityBodyColorRegExp)
    .optional()
    .describe("A color in hexadecimal format"),
});

export const UpdatePriorityDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all statuses.
 * @summary Get statuses
 */
export const listStatusesQueryMaxResultsDefault = 10;

export const listStatusesQueryStartAtDefault = 0;
export const listStatusesQueryStartAtMin = 0;
export const listStatusesQueryStartAtMax = 1000000;

export const listStatusesQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const ListStatusesQueryParams = zod.object({
  maxResults: zod
    .number()
    .min(1)
    .default(listStatusesQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listStatusesQueryStartAtMin)
    .max(listStatusesQueryStartAtMax)
    .default(listStatusesQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
  projectKey: zod
    .string()
    .regex(listStatusesQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  statusType: zod
    .enum(["TEST_CASE", "TEST_PLAN", "TEST_CYCLE", "TEST_EXECUTION"])
    .optional()
    .describe("Determines which type of entity the status belongs to."),
});

export const listStatuses200ResponseOneStartAtMin = 0;

export const listStatuses200ResponseOneTotalMin = 0;

export const listStatuses200ResponseTwoValuesItemOneNameMax = 255;

export const listStatuses200ResponseTwoValuesItemOneNameRegExp = /^(?!\\s*$).+/;
export const listStatuses200ResponseTwoValuesItemOneIndexMin = 0;

export const listStatuses200ResponseTwoValuesItemTwoArchivedDefault = false;
export const listStatuses200ResponseTwoValuesItemTwoDefaultDefault = false;

export const ListStatuses200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listStatuses200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listStatuses200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            name: zod
              .string()
              .min(1)
              .max(listStatuses200ResponseTwoValuesItemOneNameMax)
              .regex(listStatuses200ResponseTwoValuesItemOneNameRegExp),
            description: zod
              .string()
              .nullish()
              .describe("Description outlining the scope."),
            index: zod
              .number()
              .min(listStatuses200ResponseTwoValuesItemOneIndexMin),
            color: zod.string().optional(),
            archived: zod
              .boolean()
              .default(listStatuses200ResponseTwoValuesItemTwoArchivedDefault)
              .describe(
                "Determines whether the status is archived. Archived statuses are read-only and cannot be assigned to entities.",
              ),
            default: zod
              .boolean()
              .default(listStatuses200ResponseTwoValuesItemTwoDefaultDefault),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListStatusesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates a status.
 * @summary Create a status
 */
export const createStatusBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createStatusBodyNameMax = 255;

export const createStatusBodyDescriptionMax = 255;

export const createStatusBodyColorRegExp = /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;

export const CreateStatusBody = zod.object({
  projectKey: zod
    .string()
    .regex(createStatusBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createStatusBodyNameMax)
    .describe("The status name."),
  type: zod
    .enum(["TEST_CASE", "TEST_PLAN", "TEST_CYCLE", "TEST_EXECUTION"])
    .describe("Determines which type of entity the status belongs to."),
  description: zod
    .string()
    .min(1)
    .max(createStatusBodyDescriptionMax)
    .optional()
    .describe("The status description."),
  color: zod
    .string()
    .regex(createStatusBodyColorRegExp)
    .optional()
    .describe("A color in hexadecimal format"),
});

export const CreateStatus201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateStatusDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns a status for the given ID.
 * @summary Get status
 */

export const GetStatusParams = zod.object({
  statusId: zod.number().min(1),
});

export const getStatus200ResponseOneNameMax = 255;

export const getStatus200ResponseOneNameRegExp = /^(?!\\s*$).+/;
export const getStatus200ResponseOneIndexMin = 0;

export const getStatus200ResponseTwoArchivedDefault = false;
export const getStatus200ResponseTwoDefaultDefault = false;

export const GetStatus200Response = zod
  .object({
    id: zod.number().min(1),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    name: zod
      .string()
      .min(1)
      .max(getStatus200ResponseOneNameMax)
      .regex(getStatus200ResponseOneNameRegExp),
    description: zod
      .string()
      .nullish()
      .describe("Description outlining the scope."),
    index: zod.number().min(getStatus200ResponseOneIndexMin),
    color: zod.string().optional(),
    archived: zod
      .boolean()
      .default(getStatus200ResponseTwoArchivedDefault)
      .describe(
        "Determines whether the status is archived. Archived statuses are read-only and cannot be assigned to entities.",
      ),
    default: zod.boolean().default(getStatus200ResponseTwoDefaultDefault),
  })
  .strict();

export const GetStatus404Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const GetStatusDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Update an existing status.  Please take into account that for each non-specified field the value will be cleared.

 * @summary Update status
 */

export const UpdateStatusParams = zod.object({
  statusId: zod.number().min(1),
});

export const updateStatusBodyNameMax = 255;

export const updateStatusBodyDescriptionMax = 255;

export const updateStatusBodyIndexMin = 0;

export const updateStatusBodyColorRegExp = /#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})/;

export const UpdateStatusBody = zod.object({
  id: zod.number().min(1),
  project: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .describe("ID and link relative to Zephyr project."),
  name: zod
    .string()
    .min(1)
    .max(updateStatusBodyNameMax)
    .describe("The status name."),
  description: zod
    .string()
    .min(1)
    .max(updateStatusBodyDescriptionMax)
    .optional()
    .describe("The status description."),
  index: zod.number().min(updateStatusBodyIndexMin),
  archived: zod.boolean(),
  default: zod.boolean(),
  color: zod
    .string()
    .regex(updateStatusBodyColorRegExp)
    .optional()
    .describe("A color in hexadecimal format"),
});

export const UpdateStatusDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns all environments.
 * @summary Get environments
 */
export const listEnvironmentsQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const listEnvironmentsQueryMaxResultsDefault = 10;

export const listEnvironmentsQueryStartAtDefault = 0;
export const listEnvironmentsQueryStartAtMin = 0;
export const listEnvironmentsQueryStartAtMax = 1000000;

export const ListEnvironmentsQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(listEnvironmentsQueryProjectKeyRegExp)
    .optional()
    .describe("Jira project key filter"),
  maxResults: zod
    .number()
    .min(1)
    .default(listEnvironmentsQueryMaxResultsDefault)
    .describe(
      "Specifies the maximum number of results to return in a single call. The default value is 10, and the maximum value that can be requested is 1000.\n\nNote that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints. If this happens, the result set may be truncated. Always check the maxResults value in the response to confirm how many results were actually returned.\n",
    ),
  startAt: zod
    .number()
    .min(listEnvironmentsQueryStartAtMin)
    .max(listEnvironmentsQueryStartAtMax)
    .default(listEnvironmentsQueryStartAtDefault)
    .describe(
      "Zero-indexed starting position. Should be a multiple of maxResults.",
    ),
});

export const listEnvironments200ResponseOneStartAtMin = 0;

export const listEnvironments200ResponseOneTotalMin = 0;

export const listEnvironments200ResponseTwoValuesItemOneNameMax = 255;

export const listEnvironments200ResponseTwoValuesItemOneNameRegExp =
  /^(?!\\s*$).+/;
export const listEnvironments200ResponseTwoValuesItemOneIndexMin = 0;

export const listEnvironments200ResponseTwoValuesItemTwoArchivedDefault = false;

export const ListEnvironments200Response = zod
  .object({
    next: zod
      .string()
      .url()
      .nullish()
      .describe(
        "URL to the next page of results, or null if there are no more results.",
      ),
    startAt: zod
      .number()
      .min(listEnvironments200ResponseOneStartAtMin)
      .describe(
        "Indicates the index of the first item returned in the page of results.",
      ),
    maxResults: zod
      .number()
      .min(1)
      .describe(
        "Indicates the maximum number of results in this response. Note that the server may enforce a lower limit than requested, depending on resource availability or other internal constraints.",
      ),
    total: zod
      .number()
      .min(listEnvironments200ResponseOneTotalMin)
      .optional()
      .describe(
        "Indicates the total number of items available across all pages.",
      ),
    isLast: zod
      .boolean()
      .optional()
      .describe("Indicates if this is the last page of results."),
    values: zod
      .array(
        zod
          .object({
            id: zod.number().min(1),
            project: zod
              .object({
                id: zod.number().min(1),
                self: zod
                  .string()
                  .url()
                  .optional()
                  .describe(
                    "The REST API endpoint to get more resource details.",
                  ),
              })
              .strict()
              .describe("ID and link relative to Zephyr project."),
            name: zod
              .string()
              .min(1)
              .max(listEnvironments200ResponseTwoValuesItemOneNameMax)
              .regex(listEnvironments200ResponseTwoValuesItemOneNameRegExp),
            description: zod
              .string()
              .nullish()
              .describe("Description outlining the scope."),
            index: zod
              .number()
              .min(listEnvironments200ResponseTwoValuesItemOneIndexMin),
            archived: zod
              .boolean()
              .default(
                listEnvironments200ResponseTwoValuesItemTwoArchivedDefault,
              ),
          })
          .strict(),
      )
      .optional(),
  })
  .strict();

export const ListEnvironmentsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Creates an environment.
 * @summary Create an environment
 */
export const createEnvironmentBodyProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createEnvironmentBodyNameMax = 255;

export const createEnvironmentBodyDescriptionMax = 255;

export const CreateEnvironmentBody = zod.object({
  projectKey: zod
    .string()
    .regex(createEnvironmentBodyProjectKeyRegExp)
    .describe("Jira project key."),
  name: zod
    .string()
    .min(1)
    .max(createEnvironmentBodyNameMax)
    .describe("The environment name."),
  description: zod
    .string()
    .min(1)
    .max(createEnvironmentBodyDescriptionMax)
    .optional()
    .describe("The environment description."),
});

export const CreateEnvironment201Response = zod
  .object({
    id: zod.number().min(1).optional(),
    self: zod.string().optional(),
  })
  .strict();

export const CreateEnvironment400Response = zod.union([
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
]);

export const CreateEnvironmentDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Returns an environment for the given ID.
 * @summary Get environment
 */

export const GetEnvironmentParams = zod.object({
  environmentId: zod.number().min(1),
});

export const getEnvironment200ResponseOneNameMax = 255;

export const getEnvironment200ResponseOneNameRegExp = /^(?!\\s*$).+/;
export const getEnvironment200ResponseOneIndexMin = 0;

export const getEnvironment200ResponseTwoArchivedDefault = false;

export const GetEnvironment200Response = zod
  .object({
    id: zod.number().min(1),
    project: zod
      .object({
        id: zod.number().min(1),
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      })
      .strict()
      .describe("ID and link relative to Zephyr project."),
    name: zod
      .string()
      .min(1)
      .max(getEnvironment200ResponseOneNameMax)
      .regex(getEnvironment200ResponseOneNameRegExp),
    description: zod
      .string()
      .nullish()
      .describe("Description outlining the scope."),
    index: zod.number().min(getEnvironment200ResponseOneIndexMin),
    archived: zod
      .boolean()
      .default(getEnvironment200ResponseTwoArchivedDefault),
  })
  .strict();

export const GetEnvironment404Response = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

export const GetEnvironmentDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Update an existing environment.  Please take into account that for each non-specified field the value will be cleared.

 * @summary Update an environment
 */

export const UpdateEnvironmentParams = zod.object({
  environmentId: zod.number().min(1),
});

export const updateEnvironmentBodyNameMax = 255;

export const updateEnvironmentBodyDescriptionMax = 255;

export const updateEnvironmentBodyIndexMin = 0;

export const UpdateEnvironmentBody = zod.object({
  id: zod.number().min(1),
  project: zod
    .object({
      id: zod.number().min(1),
    })
    .describe("The ID of the resource")
    .and(
      zod.object({
        self: zod
          .string()
          .url()
          .optional()
          .describe("The REST API endpoint to get more resource details."),
      }),
    )
    .describe("ID and link relative to Zephyr project."),
  name: zod
    .string()
    .min(1)
    .max(updateEnvironmentBodyNameMax)
    .describe("The environment name."),
  description: zod
    .string()
    .min(1)
    .max(updateEnvironmentBodyDescriptionMax)
    .optional()
    .describe("The environment description."),
  index: zod.number().min(updateEnvironmentBodyIndexMin),
});

export const UpdateEnvironment400Response = zod.union([
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
  zod
    .object({
      errorCode: zod.number(),
      message: zod.string(),
    })
    .strict(),
]);

export const UpdateEnvironmentDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Deletes a link for the given ID.
 * @summary Delete link
 */

export const DeleteLinkParams = zod.object({
  linkId: zod.number().min(1),
});

export const DeleteLinkDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Get test case keys and versions linked to the given Jira issue.
 * @summary Get test cases
 */
export const getIssueLinkTestCasesPathIssueKeyRegExp = /.+-[0-9]+/;

export const GetIssueLinkTestCasesParams = zod.object({
  issueKey: zod
    .string()
    .regex(getIssueLinkTestCasesPathIssueKeyRegExp)
    .describe("The key of the Jira issue"),
});

export const GetIssueLinkTestCases200ResponseItem = zod
  .object({
    key: zod.string().optional(),
    version: zod.number().optional(),
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
  })
  .strict();
export const GetIssueLinkTestCases200Response = zod.array(
  GetIssueLinkTestCases200ResponseItem,
);

export const GetIssueLinkTestCasesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Get test cycle IDs linked to the given Jira issue.
 * @summary Get test cycles
 */
export const getIssueLinkTestCyclesPathIssueKeyRegExp = /.+-[0-9]+/;

export const GetIssueLinkTestCyclesParams = zod.object({
  issueKey: zod
    .string()
    .regex(getIssueLinkTestCyclesPathIssueKeyRegExp)
    .describe("The key of the Jira issue"),
});

export const GetIssueLinkTestCycles200ResponseItem = zod
  .object({
    id: zod.number().min(1),
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
  })
  .strict();
export const GetIssueLinkTestCycles200Response = zod.array(
  GetIssueLinkTestCycles200ResponseItem,
);

export const GetIssueLinkTestCyclesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Get test plan IDs linked to the given Jira issue.
 * @summary Get test plans
 */
export const getIssueLinkTestPlansPathIssueKeyRegExp = /.+-[0-9]+/;

export const GetIssueLinkTestPlansParams = zod.object({
  issueKey: zod
    .string()
    .regex(getIssueLinkTestPlansPathIssueKeyRegExp)
    .describe("The key of the Jira issue"),
});

export const GetIssueLinkTestPlans200ResponseItem = zod
  .object({
    id: zod.number().min(1),
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
  })
  .strict();
export const GetIssueLinkTestPlans200Response = zod.array(
  GetIssueLinkTestPlans200ResponseItem,
);

export const GetIssueLinkTestPlansDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Get test execution IDs linked to the given Jira issue.
 * @summary Get test executions
 */
export const getIssueLinkTestExecutionsPathIssueKeyRegExp = /.+-[0-9]+/;

export const GetIssueLinkTestExecutionsParams = zod.object({
  issueKey: zod
    .string()
    .regex(getIssueLinkTestExecutionsPathIssueKeyRegExp)
    .describe("The key of the Jira issue"),
});

export const GetIssueLinkTestExecutions200ResponseItem = zod
  .object({
    id: zod.number().min(1),
    self: zod
      .string()
      .url()
      .optional()
      .describe("The REST API endpoint to get more resource details."),
  })
  .strict();
export const GetIssueLinkTestExecutions200Response = zod.array(
  GetIssueLinkTestExecutions200ResponseItem,
);

export const GetIssueLinkTestExecutionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Create results using Zephyr's custom results format.
 * @summary Custom format
 */
export const createCustomExecutionsQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createCustomExecutionsQueryAutoCreateTestCasesDefault = false;

export const CreateCustomExecutionsQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(createCustomExecutionsQueryProjectKeyRegExp)
    .describe("Jira project key filter"),
  autoCreateTestCases: zod
    .boolean()
    .default(createCustomExecutionsQueryAutoCreateTestCasesDefault)
    .describe("Indicate if test cases should be created if non existent."),
});

export const CreateCustomExecutionsHeader = zod.object({
  "content-length": zod
    .number()
    .describe(
      "The content-length header indicates the size of the message body, in bytes. https://datatracker.ietf.org/doc/html/rfc7230#section-3.3.2",
    ),
});

export const createCustomExecutionsBodyTestCycleNameMax = 255;

export const createCustomExecutionsBodyTestCycleNameRegExp = /^(?!\\s*$).+/;

export const CreateCustomExecutionsBody = zod.object({
  file: zod.instanceof(File),
  testCycle: zod
    .object({
      name: zod
        .string()
        .min(1)
        .max(createCustomExecutionsBodyTestCycleNameMax)
        .regex(createCustomExecutionsBodyTestCycleNameRegExp)
        .optional(),
      description: zod
        .string()
        .nullish()
        .describe("Description outlining the scope."),
      jiraProjectVersion: zod
        .number()
        .min(1)
        .optional()
        .describe(
          "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
        ),
      folderId: zod
        .number()
        .min(1)
        .optional()
        .describe("ID of a folder to place the entity within."),
      customFields: zod
        .record(zod.string(), zod.unknown())
        .optional()
        .describe(
          "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
        ),
    })
    .optional()
    .describe(
      "Pass this object as a JSON in your form-data alongside the file. Make sure you set the type of this object as `application/json` otherwise the object will be ignored.",
    ),
});

export const createCustomExecutions200ResponseTestCycleKeyRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateCustomExecutions200Response = zod
  .object({
    testCycle: zod
      .object({
        id: zod
          .number()
          .min(1)
          .optional()
          .describe("The id of the newly created test cycle."),
        url: zod
          .string()
          .optional()
          .describe("The URL pointing to the newly created test cycle."),
        key: zod
          .string()
          .regex(createCustomExecutions200ResponseTestCycleKeyRegExp)
          .optional()
          .describe("The key of the newly created test cycle."),
      })
      .strict()
      .optional(),
  })
  .strict();

export const CreateCustomExecutionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Create results using the Cucumber results format.
 * @summary Cucumber format
 */
export const createCucumberExecutionsQueryProjectKeyRegExp =
  /([A-Z][A-Z_0-9]+)/;
export const createCucumberExecutionsQueryAutoCreateTestCasesDefault = false;

export const CreateCucumberExecutionsQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(createCucumberExecutionsQueryProjectKeyRegExp)
    .describe("Jira project key filter"),
  autoCreateTestCases: zod
    .boolean()
    .default(createCucumberExecutionsQueryAutoCreateTestCasesDefault)
    .describe("Indicate if test cases should be created if non existent."),
});

export const CreateCucumberExecutionsHeader = zod.object({
  "content-length": zod
    .number()
    .describe(
      "The content-length header indicates the size of the message body, in bytes. https://datatracker.ietf.org/doc/html/rfc7230#section-3.3.2",
    ),
});

export const createCucumberExecutionsBodyTestCycleNameMax = 255;

export const createCucumberExecutionsBodyTestCycleNameRegExp = /^(?!\\s*$).+/;

export const CreateCucumberExecutionsBody = zod.object({
  file: zod.instanceof(File),
  testCycle: zod
    .object({
      name: zod
        .string()
        .min(1)
        .max(createCucumberExecutionsBodyTestCycleNameMax)
        .regex(createCucumberExecutionsBodyTestCycleNameRegExp)
        .optional(),
      description: zod
        .string()
        .nullish()
        .describe("Description outlining the scope."),
      jiraProjectVersion: zod
        .number()
        .min(1)
        .optional()
        .describe(
          "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
        ),
      folderId: zod
        .number()
        .min(1)
        .optional()
        .describe("ID of a folder to place the entity within."),
      customFields: zod
        .record(zod.string(), zod.unknown())
        .optional()
        .describe(
          "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
        ),
    })
    .optional()
    .describe(
      "Pass this object as a JSON in your form-data alongside the file. Make sure you set the type of this object as `application/json` otherwise the object will be ignored.",
    ),
});

export const createCucumberExecutions200ResponseTestCycleKeyRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateCucumberExecutions200Response = zod
  .object({
    testCycle: zod
      .object({
        id: zod
          .number()
          .min(1)
          .optional()
          .describe("The id of the newly created test cycle."),
        url: zod
          .string()
          .optional()
          .describe("The URL pointing to the newly created test cycle."),
        key: zod
          .string()
          .regex(createCucumberExecutions200ResponseTestCycleKeyRegExp)
          .optional()
          .describe("The key of the newly created test cycle."),
      })
      .strict()
      .optional(),
  })
  .strict();

export const CreateCucumberExecutionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * Create results using the JUnit XML results format. Optionally, you can send a `testCycle` part in your form data to customize the created test cycle.
 * @summary JUnit XML format
 */
export const createJUnitExecutionsQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;
export const createJUnitExecutionsQueryAutoCreateTestCasesDefault = false;

export const CreateJUnitExecutionsQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(createJUnitExecutionsQueryProjectKeyRegExp)
    .describe("Jira project key filter"),
  autoCreateTestCases: zod
    .boolean()
    .default(createJUnitExecutionsQueryAutoCreateTestCasesDefault)
    .describe("Indicate if test cases should be created if non existent."),
});

export const CreateJUnitExecutionsHeader = zod.object({
  "content-length": zod
    .number()
    .describe(
      "The content-length header indicates the size of the message body, in bytes. https://datatracker.ietf.org/doc/html/rfc7230#section-3.3.2",
    ),
});

export const createJUnitExecutionsBodyTestCycleNameMax = 255;

export const createJUnitExecutionsBodyTestCycleNameRegExp = /^(?!\\s*$).+/;

export const CreateJUnitExecutionsBody = zod.object({
  file: zod.instanceof(File),
  testCycle: zod
    .object({
      name: zod
        .string()
        .min(1)
        .max(createJUnitExecutionsBodyTestCycleNameMax)
        .regex(createJUnitExecutionsBodyTestCycleNameRegExp)
        .optional(),
      description: zod
        .string()
        .nullish()
        .describe("Description outlining the scope."),
      jiraProjectVersion: zod
        .number()
        .min(1)
        .optional()
        .describe(
          "Jira Project Version ID. Relates to 'Version' or 'Releases' in Jira projects.",
        ),
      folderId: zod
        .number()
        .min(1)
        .optional()
        .describe("ID of a folder to place the entity within."),
      customFields: zod
        .record(zod.string(), zod.unknown())
        .optional()
        .describe(
          "Multi-line text fields support HTML and should denote new lines with the \\<br\\> tag.\nDates should be in the format 'yyyy-MM-dd'.\nUsers should have values of Jira User Account IDs.\n",
        ),
    })
    .optional()
    .describe(
      "Pass this object as a JSON in your form-data alongside the file. Make sure you set the type of this object as `application/json` otherwise the object will be ignored.",
    ),
});

export const createJUnitExecutions200ResponseTestCycleKeyRegExp =
  /([A-Z][A-Z_0-9]+-R[0-9]+)/;

export const CreateJUnitExecutions200Response = zod
  .object({
    testCycle: zod
      .object({
        id: zod
          .number()
          .min(1)
          .optional()
          .describe("The id of the newly created test cycle."),
        url: zod
          .string()
          .optional()
          .describe("The URL pointing to the newly created test cycle."),
        key: zod
          .string()
          .regex(createJUnitExecutions200ResponseTestCycleKeyRegExp)
          .optional()
          .describe("The key of the newly created test cycle."),
      })
      .strict()
      .optional(),
  })
  .strict();

export const CreateJUnitExecutionsDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * @summary Retrieve a zip file containing Cucumber Feature Files that matches the query passed as parameter.
 */
export const retrieveBDDTestCasesQueryProjectKeyRegExp = /([A-Z][A-Z_0-9]+)/;

export const RetrieveBDDTestCasesQueryParams = zod.object({
  projectKey: zod
    .string()
    .regex(retrieveBDDTestCasesQueryProjectKeyRegExp)
    .describe("Jira project key filter"),
});

export const RetrieveBDDTestCasesHeader = zod.object({
  Accept: zod.enum(["application/zip"]),
});

export const RetrieveBDDTestCasesDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();

/**
 * @summary Check the health of this API
 */
export const HealthcheckDefaultResponse = zod
  .object({
    errorCode: zod.number(),
    message: zod.string(),
  })
  .strict();
